# Phase 7 — 45차 핸드오프

**일자** 2026-08-06
**주제** 회원 연락처 경로 통일 (44차 이월 7-1-1) · CLAUDE.md 규약 정정 배포
**커밋** `6b6b3d6`(docs) → `6d0610c`(fix)
**배포** prod `6d0610c` 반영 완료 (pm2 ↺ 36) — **dev·prod 완전 일치(격차 0)**

---

## 0. 세션 시작 프로토콜 (다음 세션은 여기서 시작)

```bash
pm2 ls                                                    # online, ↺ 36
curl -so /dev/null -w "%{http_code}\n" localhost:3000     # 200
cd ~/apps/mysado-shop      && git log --oneline -1         # 6d0610c
cd ~/apps/mysado-shop-prod && git log --oneline -1         # 6d0610c (일치)
```

> **이번엔 dev·prod가 일치합니다(한 칸 차이 없음).** 43·44차는 docs 커밋이 prod 미배포라 한 칸 뒤였으나,
> 45차는 CLAUDE.md docs 커밋(`6b6b3d6`)을 이번 기능 배포에 **동반**시켜 격차를 0으로 닫았습니다.
> 단, **이 핸드오프 문서를 커밋하면** 그 순간 dev가 한 칸 앞서므로 다음 세션 기대값은 이 문서 커밋 해시가 됩니다
> (44차 §8-⑹ 반복).

DB 무접근 세션. 43차 §0 재고 정합 쿼리(`prod-004`=195, `prod-017`=98)는 그대로 유효
(세션 중 프로필 테스트로 `cha_01` **테스트 계정**의 phone_number만 변경됨 — §5 참고, product 재고 무관).

---

## 1. 이번 세션의 성격 — 44차의 나머지 절반

44차가 **배송지 경로**에서 고친 연락처 정규화 버그(`010.1234.5678` 같은 유효 입력이 400 탈락)가
**회원 경로 3곳**에 그대로 살아 있었습니다. 44차가 `lib/validation/contact.ts`에 회원용
함수·상수를 **정의만** 해 두었으므로(주석: "44차에는 정의만 둔다"), 45차는 그 호출부를 연결했습니다.

44·43차 §8-⑴("착수 전 실측이 설계를 바꾼다")이 이번에도 값을 했습니다. 실측으로 드러난 것:

- 44차 §7-1-1이 "서버 검증"이라 부른 `signup/page.tsx`는 실제로 **`"use client"` 클라이언트**였습니다.
  서버 검증은 Better Auth 내부에 있고 우리 코드가 아닙니다. **회원가입의 우리 검증은 클라이언트뿐.**
- 세 곳(`signup`·`profile/route.ts`·`ProfileForm.tsx`)의 현재 규칙이 **전부 동일** `/^010\d{8}$/` +
  `[-\s]` 정규화였습니다(복제 3벌). `route.ts`만 진짜 서버 검증 지점.
- **제5의 사실**: 표시용 공용(`lib/format-phone.ts:19`)과 검증용 공용(`contact.ts:15`)이 이미
  둘 다 `[^0-9]`로 수렴해 있었습니다. 회원 3곳의 `[-\s]`만 바꾸면 프로젝트 전체가 단일 정규화로 통일.

---

## 2. 확정된 설계 (Chris 결정 4건)

| # | 논점 | 결정 | 근거 |
|---|---|---|---|
| ① | 접두 규칙 | **`010` 전용 → `isValidMemberPhone`(`^01[016789]\d{7,8}$`)** | 44차 §2-1이 회원=휴대폰 전용을 이 규칙으로 확정. 011번대도 SMS 수신 가능 |
| ② | 에러 문구 | **`MEMBER_PHONE_ERROR` 단일화** | 서버 400과 폼 인라인이 같은 상수(44차 원칙의 회원판) |
| ③ | 로컬 `formatPhone` | **제거 → 공용 `lib/format-phone`** | "같은 이름 다른 동작"(로컬 11자리만) 해소 |
| ④ | 작업 방식 | **Claude Code 위임** | 42·44차 하이브리드 표준 |

### 2-1. 검증은 원본에, 저장은 정규화값에

`isValidMemberPhone`이 내부에서 `normalizePhone`을 이미 호출하므로 **검증은 원본 입력**에 맡기고
(`isValidMemberPhone(phone)`), **저장·전송값만** `phoneDigits = normalizePhone(phone)`으로 명시.
세 곳 모두 이미 `phoneNumber: phoneDigits`를 전송하고 있어 변수만 유지하면 됐습니다.
44차 §4-⑶(검증값=저장값 일치)이 원천적으로 성립.

### 2-2. 회원 UNIQUE는 이번에도 안 넣음 (44차 §2-2 승계)

전화번호 UNIQUE는 SMS 본인인증과 한 묶음(로드맵 **10-6**). 이번은 검증 규칙만.
44차가 확인한 대로 현재 회원 데이터는 전부 정규형이라, 이 검증이 붙으면 10-6 착수 시 선행 정리 불필요.

---

## 3. 변경 내역

```
app/signup/page.tsx                 정규화·검증·문구 → 공용 호출 (+import)
app/api/mypage/profile/route.ts     동일 (서버 라우트, 유일한 우리 서버 검증)
app/mypage/profile/ProfileForm.tsx  동일 + 로컬 formatPhone 제거 (+import 2종)
CLAUDE.md                           §6 규약 정정 (별건, 6b6b3d6)
prisma/schema.prisma                무수정 — 마이그레이션 0건
```

3파일 +15/−17. 삭제 우위는 로컬 `formatPhone` 5줄 제거 때문.

---

## 4. 🔴 45차 신규 함정 — exact-match 앵커의 빈 줄 누락

**이번 세션 최대 교훈.** C-1 find 블록이 **0건**으로 막혔습니다.

원인: 지시서 앵커에 빈 줄이 없었으나 실제 `ProfileForm.tsx`에는 빈 줄 3개(1행·4행·10행 뒤)가
있었습니다. exact-match는 빈 줄 하나까지 일치해야 하므로 전체가 0건. Claude Code가
**count==0을 감지하고 정상 중단·보고**했습니다(count 가드가 의도대로 작동).

- 44차 §5-2("grep 0건은 '없다'가 아니라 '패턴 오류'일 수 있다")의 **앵커판**. 0건이 "코드 없음"이
  아니라 "내 앵커가 빈 줄을 빠뜨림"이었습니다.
- 원인 규명은 `sed`가 아니라 **`cat -n`**으로 했습니다. `cat -n`은 빈 줄에도 행번호를 매겨
  빈 줄 위치가 눈에 보입니다. **앵커 작성 전 `cat -n`이 `sed`보다 안전합니다.**

**해결**: 하나의 큰 앵커(import + interface + formatPhone 통째)를 **2건으로 분할**.
import 추가와 formatPhone 제거는 원래 파일에서 떨어져 있고 그 사이에 interface(손대면 안 됨)가
있었으므로, 성격대로 나누는 것이 정답이었습니다. interface를 앵커에서 빼니 빈 줄 리스크도 절반.

> **규약 (45차 신설)**: exact-match 앵커는 빈 줄까지 포함해 실측한다. 파일 상단처럼 빈 줄이 섞이는
> 구간은 **앵커 작성 전 `cat -n`으로 빈 줄 위치를 확인**한다. 큰 블록 하나보다 **작은 앵커 여럿**이
> 빈 줄 리스크가 낮다(interface 등 무관 코드를 앵커에 포함하지 말 것).
> B-1처럼 "실측 행번호"를 앵커 주석에 남기면 조기에 드러난다.

### 4-1. Claude Code 산출물 평가 — 42·44차 수준 유지

- A·B 4블록 count==1 즉시 성공, C만 0건에서 **강제 적용(3번) 대신 중단·질의** — 절대 규칙 준수.
- §5 자기검증에서 **"0건이 패턴 오류가 아님"을 편집 전 버전(`git show HEAD:<파일>`)에 grep 걸어
  역증명** — 지시서가 요구한 것 이상. 44차 §5-2 교훈을 스스로 적용.
- 스스로 고친 것 없음. C-1b 삭제 범위에 함수 뒤 빈 줄 1개 포함한 재량만 명시(빈 줄 2연속 방지).
- `rm` deny로 임시 파일(`/tmp/.../scratchpad/count45.py`) 못 지움 — **repo 밖(`/tmp`)이라 무해**,
  커밋 위험 없음. 42차 §7-1의 `.tmp-*`(repo 루트)와 달리 gitignore 불필요.

---

## 5. 검증 기록

### 5-1. 정적 검사 (dev, 독립 실측)

```
npx tsc --noEmit        에러 0 (우리가 직접 재실행 — agent 보고와 일치)
npx next lint --file    3파일 전부 ✔
구 코드 잔존            /^010·로컬 formatPhone·[-\s] → 회원 3파일에서 0건
호출부 실측            3파일 전부 import + normalizePhone/isValidMemberPhone/MEMBER_PHONE_ERROR
빈 줄 서식             ProfileForm 상단 cat -n 확인 — C-1b 삭제 후 정상
git status --porcelain 수정 3파일뿐 (범위 밖 편집 0)
```

### 5-2. 브라우저 E2E 4/4 통과 (dev)

| # | 위치 | 입력 | 결과 |
|---|---|---|---|
| ① | `/signup` | `02-123-4567` (유선) | **탈락** + `MEMBER_PHONE_ERROR` ✅ |
| ② | `/mypage/profile` | `010.1234.5678` | **저장 성공** ✅ (44차 버그의 회원판 수정 실증) |
| ③ | `/mypage/profile` | `0511234567` (지역번호) | **탈락** + `MEMBER_PHONE_ERROR` ✅ |
| ④ | `/mypage/profile` | `011.234.7894` | **저장 성공** ✅ (접두 확장 + 정규화 관대화 동시 실증) |

②가 세션 핵심, ④가 동작 확장(`010`→`01X`)의 직접 증거. 가입 폼의 `010.점` 통과 화면은
미촬영이나 프로필과 동일 `isValidMemberPhone` 경유라 논리 보장.

> **미검증 명시**: 가입 폼 **정상 통과 후 실제 계정 생성** 경로는 안 밟음(테스트 계정 누적 방지).
> curl 교차검증은 클라이언트 컴포넌트라 안 닿음(44차 §5-4) — 브라우저 육안이 유일 수단.

### 5-3. prod 배포

```
push        16e81e8..6d0610c
prod pull   1e354bf..6d0610c  Fast-forward (CLAUDE.md·핸드오프44·기능3파일 동반)
build       ✓ Compiled / ✓ types / 37 pages (신규 라우트 0)
restart     pm2 ↺ 35 → 36 / pm2 save (dump.pm2 기록)
curl        localhost:3000 200
정합        dev·prod 둘 다 6d0610c — 격차 0
```

---

## 6. CLAUDE.md 규약 정정 (커밋 `6b6b3d6`, 세션 서두 별건)

44차 §7-1-7이 지시한 정본 규약 정정 중 **2건**을 CLAUDE.md에 반영:

- **§6-2 `user` 컬럼 규칙** — "Better Auth 테이블=camelCase"가 틀렸음(`phoneNumber`는 미존재,
  실제 `phone_number`). "코어 필드 camelCase + 프로젝트 추가 필드 snake_case, 한 테이블 혼재"로
  정정. FK 갈림(`product_view_log`·`search_log`=`"userId"` / `address`·`cart_item`·`orders`=`user_id`)도 반영.
- **§6-1 `!~` 회피** — `psql -c`에서도 히스토리 확장 발생. `NOT (컬럼 ~ '패턴')` 대체 명시.

**§5-4(curl 검증 한계)는 미반영** — gotcha가 아니라 검증 방법론이라 CLAUDE.md 결에 안 맞음.
세션 검증 규약(핸드오프 계열)에 두는 것이 적절.

> **아이러니**: 이 커밋을 처음 만들 때 커밋 **메시지 안의 `!~`**가 히스토리 확장으로 치환돼
> 메시지에 쓰레기 문자열이 박혔습니다. `--amend`로 복구. **커밋 메시지에도 `!` 금지**가 이번 교훈.
> 44차 §6-1이 psql·node를 짚었으나 git commit 메시지도 같은 셸 경로임을 놓쳤던 것.

---

## 7. 이월 항목

### 7-1. 신규 (45차 발생)

| # | 항목 | 비고 |
|---|---|---|
| 1 | 커밋 메시지 `!` 확장 | §6 아이러니. **커밋 메시지에서도 `!` 금지** — CLAUDE.md 셸 절에 추가 후보 |
| 2 | 국제번호 비일관(44차 §7-1-4 승계) | `+82 10-…`(12자리) 탈락 / `+05…` 우연 통과. 회원 경로도 동일 — 규칙 손대면 재검증 필요, 범위 밖 유지 |
| 3 | curl 검증 한계 문서화 위치 | CLAUDE.md 아닌 세션 검증 규약에 둘 것(§6). 미반영 상태 |

### 7-2. 기존 이월 (44차에서 승계)

- **회원 연락처 경로 통일 (7-1-1) → 45차 완료** ✅
- 장바구니 수량 직접 입력(44차 7-1-2) — 독립 세션 분량. `−`/`+`만 가능, 재고 195개 항목 비현실적
- 퀵계좌이체 한도 초과 에러(44차 7-1-3) — 8-3 점검
- 웹훅 두 경로 미검증(43차 §4) / Prisma 트랜잭션 타임아웃 5초(43차 §5-⑵)
- 라이브 전환 후 계좌이체 취소 시 `refundReceiveAccount` 요구 가능성
- 테스트 주문 정리 `MYSADO-260804-0001·0002`(PENDING)·`0005·0007`(PAID) → **8-9**
- **🔴 기존 `PAID` 주문 미차감분 — `cancel` API 호출 금지**(43차 §5-⑶). 8-9와 코호트 분리 설계 필요
- `app/api/mypage/addresses/*` raw SQL 3건 / `01_payment.md` 결제창 방식 기준
- `product.status` DEFAULT `'SALE'` / `layer3b-write.js` V6 전역 유일성 검사
- prod-011 `로열핑/뱅` 오타 / prod-154 `model_name` / `lasertTransparent` 관례
- 42차 절단 18건(`라이트` 10·`다크` 8) / `라이트블루` 3건 / `prod-066` 투명
- 7-3 게시 게이트(draft→review→published) 미구현
- mysado.co.kr 301 / 소셜로그인 / `admin-guard.ts` 타입 캐스트

### 7-3. 🔴 토스 라이브 (기한 2026-08-16 — 10일 남음)

| 항목 | 상태 |
|---|---|
| 라이브 계약 신청 | ✅ 심사 중 (44차 시점) |
| footer 사업자정보 / 배송·교환·반품 정책 / 8-2 조건부 재고 차감 | ✅ 완료 |
| 라이브 웹훅 실발신 수신 확인 | ⚠️ 심사 통과 전 불가 |
| 키 교체 → prod 재빌드 (`NEXT_PUBLIC_*` 빌드타임) | 계약 승인 직후 |
| 전환 폴백 cuid 제거 / `customerMobilePhone` 010 가드 / 채번 동시성 재시도 | 계약 승인 직후. **010 가드는 `isValidMemberPhone` 재사용** (45차로 호출부 검증 완료) |

---

## 8. 새로 확립된 원칙

**⑴ exact-match 앵커는 빈 줄까지 실측한다** — 큰 블록 하나보다 작은 앵커 여럿이 안전.
빈 줄 섞인 구간은 `cat -n`으로 위치 확인 후 앵커 작성(§4).

**⑵ 셸 `!` 확장은 커밋 메시지에도 온다** — psql·node·git commit 전부 같은 셸 경로.
메시지에서도 `!` 배제(§6).

**⑶ 검증 왕복은 세션 성격 확정 후 한 블록으로 묶는다** — 읽기 전용 검증(tsc·grep·git status·cat)을
한 번에 실행·붙여넣기. 위험 명령(write·배포)은 여전히 하나씩 게이트. (이번 §5-1에서 적용)

**⑷ 지시서 산출물은 `~/output`으로 옮긴다** — Chris 요청. (다음 세션 지시서부터 명시)

---

## 9. 산출물

| 경로 | 내용 |
|---|---|
| `app/signup/page.tsx`·`route.ts`·`ProfileForm.tsx` | 회원 연락처 통일 (커밋 `6d0610c`) |
| `CLAUDE.md` | §6 규약 정정 (커밋 `6b6b3d6`) |
| `~/output/작업지시서-45차-회원연락처경로통일.md` | Claude Code 지시서 (gitignore) |

DB 무접근 — **백업 없음**(런타임 코드만, 되돌리기는 git). 마이그레이션 0건.
런타임 코드 변경이라 **빌드·재시작 있음**(44차형).

---

## 10. 46차 착수 후보

**A. 7-3 게시 게이트** (판정기 + 리포트 먼저) — 44차 §10-B 권고 승계. 게이트 조건에
`seoTitle`·`seoDescription`이 들어가는데 Layer 3b가 비어 통과 0건. **리포트 형태면 "몇 건이
무엇 때문에 탈락"이 숫자로 나와 Layer 3b 부채 규모의 첫 계측**이 됨.

**B. 8-9 테스트 데이터 정리** — 실결제 개시 직전이 적기. 43차 §5-⑶(미차감 주문) 코호트 분리
설계 필요(43차 이전=미차감 / 이후=차감). `cancel` 호출 시 유령 재고 주의.

**C. 장바구니 수량 직접 입력 (7-1-2)** — 독립 세션 분량. 검증(음수·0·재고초과·비숫자)+디바운스
PATCH + 43차 재고 차감 경로 연동.

> **마감**: Toss 라이브 계약 **2026-08-16 (10일 남음)** — 심사 중.
> 심사 통과 시 8-3 묶음(키 교체·폴백 제거·010 가드)이 최우선. `customerMobilePhone` 010 가드는
> 45차가 `isValidMemberPhone` 호출부를 이미 검증했으므로 재사용만 하면 됨.
