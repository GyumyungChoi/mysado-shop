# Phase 7 — 46차 핸드오프

**일자** 2026-08-08
**주제** 7-3 게시 게이트 — 판정기 + 리포트 (승격 write 없음)
**커밋** `641091e`(feat)
**배포** prod **미반영 — 의도적**. `scripts/` 전용이라 런타임 무관 (§7)

---

## 0. 세션 시작 프로토콜 (다음 세션은 여기서 시작)

```bash
pm2 ls                                                    # online, ↺ 36 (변동 없음)
curl -so /dev/null -w "%{http_code}\n" localhost:3000     # 200
cd ~/apps/mysado-shop      && git log --oneline -1         # 641091e (+ 이 문서 커밋 시 한 칸 더)
cd ~/apps/mysado-shop-prod && git log --oneline -1         # 6d0610c (두 칸 뒤 — 정상, §7)
```

> **dev·prod 격차 2칸**입니다. `78ca097`(45차 핸드오프)·`641091e`(46차 scripts) 둘 다 런타임
> 무관이라 배포 명분이 없습니다. 45차가 닫은 격차가 다시 벌어졌으나 **원인이 기록된 격차**입니다
> (42차 §2가 경고한 "모르는 사이 벌어진 격차"가 아님).

DB **읽기 전용** 세션. 43차 §0 재고 정합(`prod-004`=195, `prod-017`=98) 그대로 유효.
백업 없음 — SELECT만 수행했고 write 경로가 존재하지 않습니다.

---

## 1. 이번 세션의 핵심 발견 — 상태 축이 둘이고, 만난 적이 없다

45차 §10-A는 "게이트 조건에 `seoTitle`이 들어가는데 Layer 3b가 비어 **통과 0건**"을 예상했습니다.
착수 전 실측이 두 가지를 뒤집었습니다.

### 1-1. `content_status`는 런타임 소비자가 0명

```
grep -rn "content_status|contentStatus|CONTENT_STATUS" app/ lib/ components/   →  0건
(재확인: "content" -i 로 넓혀도 매치 4건 전부 SuccessContent·FailContent 오탐)
```

| 축 | 컬럼 | 런타임 소비 | 역할 |
|---|---|---|---|
| **판매** | `status` + `is_active` | ✅ `lib/product-status.ts` 경유 | 노출·구매를 실제로 결정 |
| **콘텐츠** | `content_status` | ❌ **없음** | 콘텐츠 성숙도 (DB에만 존재) |

`scripts/product-promote-write.js`가 이를 확증합니다 — 이름의 "승격"이 이미 **판매상태**
승격(DRAFT→ON_SALE)을 뜻하고, 헤더가 `content_status`를 **무접근 컬럼으로 명시**합니다.

> 🔴 **33차 게이트 문구를 그대로 구현했다면 사고였습니다.** "published 전환 조건"이라는 표현은
> published가 곧 게시(노출)라는 인상을 주지만, 실제 노출은 `status`가 결정하고 있습니다.
> 지금 sitemap·JSON-LD를 `published`로 필터했다면 **통과 0건이므로 230개 상품 페이지가
> 한 번에 사라집니다.**

**46차 설계 제약 (선언)**: 이 게이트는 **노출을 제어하지 않습니다.** `status`·`is_active`
무접근이며 산출물은 판정 결과와 리포트뿐입니다. 두 축의 연결은 별도 트리거에서 재논의합니다.

### 1-2. 통과 예상은 0이 아니라 42

```
content_status   draft 188 / review 42        (raw 0 · published 0)
seoTitle 부재    188   seoDescription 부재 188   highlights 부재 188
```

188이 세 번 반복되고 `draft`와 정확히 일치합니다. 게이트는 첫 실행부터 유의미한 숫자를 냅니다.

---

## 2. 확정된 설계 (Chris 결정 5건)

| # | 논점 | 결정 | 근거 |
|---|---|---|---|
| ① | 판정기 언어·위치 | **`scripts/` CommonJS 2파일** | `tsx`·`ts-node` 부재(package.json 실측). 런타임 소비자 0이라 `lib/` 배치는 미사용 모듈 |
| ② | 노출 제어 | **하지 않음.** `status`·`isVisible` 무접근 | §1-1 |
| ③ | 승격 write | **범위 밖.** 리포트까지 | 43차 §8-⑴. 첫 계측 전에 전환 규칙 고정 금지 |
| ④ | `specs.color` 조건 | **WARN 강등** | 면제 마커 부재(§3-2) |
| ⑤ | `seoTitle` 전역 유일성 | **게이트 조건에 편입(G5)** | 게이트가 어차피 전 행을 읽으므로 비용 0. `layer3b-write.js` V6 구멍의 상시 탐지망 |

### 2-1. 파일 분리 원칙

```
scripts/publish-gate.js          213줄  판정 규칙 — 순수 함수. DB·파일·console·process 무접근
scripts/publish-gate-report.js   411줄  DB 읽기 + 집계 + 출력
```

판정 규칙은 향후 런타임(TS)으로 이식될 수 있습니다. 그때 **옮길 대상이 한 파일**이 되도록
I/O를 전부 report 쪽에 가뒀습니다. 이름에서 `promote`를 피했습니다 — 판매축이 선점한 단어입니다.

---

## 3. 판정 규칙 6종

| # | 코드 | 조건 | tier | 면제 |
|---|---|---|---|---|
| G1 | `MISSING_BASIC` | `name`·`images≥1`·`price>0`·`categoryId` | FAIL | 없음 (실패 항목명을 `detail`로) |
| G2 | `MISSING_MODELS` | `compatibleModels ≥1` | FAIL | `contentMeta['compatibleModels'].source === 'none'` |
| G3 | `MISSING_SEO_TITLE` | `seoTitle` 존재(trim) | FAIL | 없음 |
| G4 | `MISSING_SEO_DESC` | `seoDescription` 존재 | FAIL | 없음 |
| G5 | `DUPLICATE_SEO_TITLE` | 전역 유일 | FAIL | 빈 값은 G3 담당이라 대상 외 |
| G6 | `MISSING_COLOR` | `specs.color` 존재 | **WARN** | — |

`highlights`는 **뺐습니다** — 33차 원문 게이트 조건에 없고, `seoTitle`과 완전 동기라 독립 정보 0.

### 3-1. G2 면제가 이 설계의 검증 지점

```
compatible_models 부재 3건   =   cm_source='none' 3건   (동일 행 실측 확인)
prod-025 A4 보안용지 / prod-171 푸바오 스마트톡 / prod-172 푸바오 키링
```

`prod-172`는 **`review`인데 모델이 비어** 있습니다. 면제가 없으면 유일한 드리프트로 잘못
적발됩니다. 폰 모델이 없는 상품이므로 통과가 정답이고, 실행 결과 G2 = 0건으로 확인됐습니다.

### 3-2. G6가 WARN인 이유 (변경 금지)

```
meta_has_color | row_has_color | count
f              | f             |   106
t              | t             |   124
```

**완전 상관**입니다. `specs.color`가 없는 106건은 *색상이 없는 상품*과 *규칙이 못 찾은 상품*이
데이터상 **구별되지 않습니다**(면제 마커가 존재하지 않음). 42차 §7-1이 이월한
"미조사 사각 ⓒ(`color IS NULL` 107건)"의 현재값(신규 31건 유입 후 106)이 바로 이것입니다.

판정 불가능한 것을 탈락시키면 리포트가 거짓 수치를 냅니다. 면제 마커 신설은 게이트가 아니라
**Layer 3b 쪽 별건**입니다.

---

## 4. 실행 결과 — 첫 계측

```
총 230 / PASS 42 / FAIL 188 / WARN 보유 106

FAIL 사유    188 MISSING_SEO_DESC
             188 MISSING_SEO_TITLE
             (G1·G2·G5 = 0건)

교차표       draft   0 PASS / 188 FAIL
             review 42 PASS /   0 FAIL

드리프트     0건 (세 군 모두 "없음" 명시 출력)
```

**Layer 3b 부채 = 188건, 사유는 단 2종.** 45차 §10-A가 기대한 "부채 규모의 첫 계측"입니다.

`content_status`가 지금까지 손으로 관리됐음에도 판정과 100% 일치합니다. 규율이 유지돼 왔다는
증거이며, 동시에 게이트의 당면 가치가 *현재 적발*이 아니라 **Layer 3b 진행 중 자동 감시**에
있음을 뜻합니다.

---

## 5. 검증 기록

### 5-1. 교차검증의 순서가 유리했다

기준선을 **psql로 먼저 뽑아두고** 스크립트가 나중에 나왔습니다. 따라서 이 일치는 스크립트가
자기 답을 채점한 것이 아니라 **독립 실측과의 대조**입니다. 42차 "자체 보고 불신" 원칙이
구조적으로 충족됐습니다.

> **기준선을 지시서에서 의도적으로 뺐습니다.** 정답이 적혀 있으면 agent가 로직 대신 숫자를
> 맞출 여지가 생깁니다(34차 원칙 ①의 연장). 검증용으로 Chris만 보유했고,
> `grep "230\|188\|106\| 42\b"` 실측 결과 **하드코딩 0줄**로 확인했습니다.

### 5-2. 정적 검사 (dev, 독립 재실측)

```
wc -l                   213 / 411 — agent 보고와 일치
git status --porcelain  ?? 신규 2개뿐 (기존 파일 수정 0)
순수성 grep             require·console·process·prisma → publish-gate.js 에서 0줄
write 경로 grep         update·create·delete·upsert·*Raw·apply → 두 파일 0줄
기대값 하드코딩         0줄
Prisma 초기화           ensureDatabaseUrl 3단(환경변수 → loadEnvFile → 경고), findMany 1곳, $disconnect 有
```

### 5-3. Claude Code 산출물 평가 — 42·44·45차 수준 유지

- §5 grep 4종 원문 제시, 요약 보고 아님. 실행 금지 규칙 준수(스크립트 미실행).
- 재량 3건을 **전부 사전 신고**하고 되돌리는 법까지 명시:
  - (a) 드리프트에 "참고군" 추가 — 근거가 *코드 리터럴 4종*이었으나 **DB 실측은 2종**
    (`raw`·`published` 0건). 항상 0을 출력하는 무해·방어적 절이라 유지. 근거 층위가
    코드였다는 점만 기록.
  - (b) G6를 "키 존재"가 아닌 "값이 채워짐"으로 해석 — `Array.isArray` 분기가 있어
    **`prod-009`(42차가 보존한 유일한 배열 `["그린","핑크"]`)가 오분류되지 않음**.
    실행 결과 WARN 106으로 기준선과 일치 → **빈 문자열·빈 배열은 실재하지 않음**이 확인됐고,
    42차 사각 ⓒ에 새 하위 유형은 없었습니다.
  - (c) G5 유일성을 trim 기준으로 — G3와 축 일치. 타당.
- 스스로 고친 것 없음. 임시 파일 없음.

---

## 6. 🔴 46차 신규 함정 — psql은 배치에 넣을 수 없다

세션 서두 검증 블록에 `psql` 2건을 넣었고 **둘 다 실패**했습니다.

```
Password for user mysado:
psql: error: ... password authentication failed for user "mysado"
```

인증 문제가 아닙니다. psql이 비밀번호를 묻는 순간 **stdin에 남아 있던 다음 줄
(`# --- 46차 실측 B ...`)을 비밀번호로 먹었습니다.** 비밀번호는 입력된 적이 없고,
두 번째 psql도 같은 방식으로 연쇄 실패했습니다.

> **규약 (46차 신설)**: 45차 원칙 ⑶("읽기 전용 검증은 한 블록")에 **예외가 있습니다.**
> 대화형 프롬프트를 띄우는 명령(psql 등)은 배치에 넣지 않고 **항상 단독 실행**합니다.
> 붙여넣기 실행에서 프롬프트는 뒤따르는 줄을 삼킵니다.

44차 §5-2("0건은 '없다'가 아니라 '패턴 오류'일 수 있다")·45차 §4(앵커 빈 줄)와 같은 계열 —
**실패 메시지가 진짜 원인을 가리키지 않는** 사례가 세 세션 연속 나왔습니다.

---

## 7. prod 미반영 판단 (의도적)

| 근거 | |
|---|---|
| `scripts/`는 prod에서 실행되지 않음 | 배포 명분 없음 |
| 런타임 코드(`app/`·`lib/`·`components/`) 무수정 | 빌드·재시작 불요 |
| pm2 ↺36 유지 | 재시작 없음 |

**다음 기능 배포 때 동반**시켜 격차를 닫습니다(42차 §2 교훈: 데이터·문서 커밋이 조용히
격차를 만든다 — 단, 이번은 **원인이 이 문서에 기록된** 격차입니다).

---

## 8. 이월 항목

### 8-1. 신규 (46차 발생)

| # | 항목 | 비고 |
|---|---|---|
| 1 | **psql 배치 금지** | §6. CLAUDE.md 셸 절 추가 후보(`!` 확장·`!~`와 같은 자리) |
| 2 | **게이트 승격 write 미구현** | 판정·리포트까지만. `content_status` 전환 로직은 미착수 |
| 3 | **두 축 연결 미정** | `content_status` ↔ 노출(sitemap·JSON-LD·`status`). 연결 시 230페이지 소멸 위험 — 명시적 설계 세션 필요 |
| 4 | 계측 스냅샷 시계열 | `--out ~/output/…` 로 배치마다 기록하면 188 감소 곡선이 남음. repo 커밋은 비권장(매 실행 diff) |
| 5 | 참고군 근거 층위 | §5-3(a). 코드 리터럴 4종 vs DB 실측 2종 |

### 8-2. 기존 이월 (45차에서 승계)

- **7-3 게시 게이트 → 46차 판정·리포트 완료** ✅ / 승격 write는 8-1-2로 이월
- `layer3b-write.js` V6 전역 유일성 — **G5가 상시 탐지망으로 대체**(write 시점 방어는 여전히 부재)
- **Layer 3b 잔여 188건** — 게이트로 규모 확정. 병목은 이미지 준비(Chris 수동)
- 장바구니 수량 직접 입력(44차 7-1-2) / 퀵계좌이체 한도 초과 에러 → 8-3 점검
- 웹훅 두 경로 미검증(43차 §4) / Prisma 트랜잭션 타임아웃 5초(43차 §5-⑵)
- 라이브 전환 후 계좌이체 취소 시 `refundReceiveAccount` 요구 가능성
- 테스트 주문 정리 `MYSADO-260804-0001·0002`(PENDING)·`0005·0007`(PAID) → **8-9**
- **🔴 기존 `PAID` 주문 미차감분 — `cancel` API 호출 금지**(43차 §5-⑶). 코호트 분리 설계 필요
- 커밋 메시지 `!` 금지 / 국제번호 비일관 / curl 검증 한계 문서화 위치
- `app/api/mypage/addresses/*` raw SQL 3건 / `01_payment.md` 결제창 방식 기준
- `product.status` DEFAULT `'SALE'` (스키마 실측 재확인 — 여전히 구값)
- prod-011 `로열핑/뱅` / prod-154 `model_name` / `lasertTransparent` 관례
- 42차 절단 18건(`라이트` 10·`다크` 8) / `라이트블루` 3건 / `prod-066` 투명
- **미조사 사각 ⓒ = 106건 확정** — 46차가 WARN으로 계측. ⓐⓑ는 미조사 유지
- mysado.co.kr 301 / 소셜로그인 / `admin-guard.ts` 타입 캐스트

### 8-3. 🔴 토스 라이브 (기한 2026-08-16 — **8일 남음**)

| 항목 | 상태 |
|---|---|
| 라이브 계약 신청 | ✅ 심사 중 |
| footer 사업자정보 / 배송·교환·반품 정책 / 8-2 조건부 재고 차감 | ✅ 완료 |
| 라이브 웹훅 실발신 수신 확인 | ⚠️ 심사 통과 전 불가 |
| 키 교체 → prod 재빌드 (`NEXT_PUBLIC_*` 빌드타임) | 계약 승인 직후 |
| 전환 폴백 cuid 제거 / `customerMobilePhone` 010 가드 / 채번 동시성 재시도 | 계약 승인 직후. 010 가드는 `isValidMemberPhone` 재사용 |

---

## 9. 새로 확립된 원칙

**⑴ 대화형 프롬프트는 배치에 넣지 않는다** — psql 비밀번호 프롬프트가 뒤 줄을 삼킨다.
45차 원칙 ⑶의 예외(§6).

**⑵ 기대값은 지시서에 적지 않는다** — 정답이 있으면 로직 대신 숫자를 맞출 여지가 생긴다.
검증자만 보유하고, 하드코딩 여부를 grep으로 확인한다(§5-1).

**⑶ 같은 이름의 축이 둘일 수 있다** — `content_status`의 "published"와 판매 `status`가
둘 다 "게시"를 연상시키지만 서로 무관했다. 로드맵 문구(33차 "published 전환 조건")를
구현 전에 **어느 축인지 실측**해야 한다(§1-1).

**⑷ 판정 불가능한 것을 탈락시키지 않는다** — 면제 마커가 없는 조건은 FAIL이 아니라 WARN.
거짓 수치를 내는 리포트는 계측 도구로서 무가치하다(§3-2).

---

## 10. 산출물

| 경로 | 내용 |
|---|---|
| `scripts/publish-gate.js` | 213줄, 순수 판정기 (커밋 `641091e`) |
| `scripts/publish-gate-report.js` | 411줄, DB 읽기·집계·리포트 (커밋 `641091e`) |
| `~/output/작업지시서-46차-게시게이트.md` | Claude Code 지시서 (gitignore) |

DB 읽기 전용 — **백업 없음**. 마이그레이션 0건. 런타임 코드 무수정 — **빌드·재시작 없음**(42차형).

---

## 11. 47차 착수 후보

**A. 8-9 테스트 데이터 정리** — 기한이 8일 남았고 실결제 개시 직전이 적기입니다.
43차 §5-⑶ 미차감 주문 코호트 분리 설계 필요(43차 이전=미차감 / 이후=차감).
`cancel` 호출 시 유령 재고 주의. **세 주문(`MYSADO-260721-0001`·`260725-0001`·`0002`)에
cancel 금지**가 여전히 유효.

**B. Layer 3b 배치 확대** — 46차가 부채를 188건으로 확정했고, 게이트가 진척을 자동 계측합니다.
배치마다 리포트를 찍으면 감소 곡선이 남습니다(8-1-4).

**C. 장바구니 수량 직접 입력 (44차 7-1-2)** — 독립 세션 분량.

> **마감**: Toss 라이브 계약 **2026-08-16 (8일 남음)** — 심사 중.
> 심사 통과 시 8-3 묶음(키 교체·폴백 제거·010 가드)이 **모든 것에 우선**합니다.
> 그 세션에 46차 커밋 2개가 prod로 동반됩니다(§7).
