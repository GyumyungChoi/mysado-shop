# Phase 7 — 47차 핸드오프

**일자** 2026-08-09
**주제** 8-9 테스트 데이터 정리 (물리 삭제) · 데이터 삭제 규약 신설
**커밋** (이 문서 + CLAUDE.md 규약 추가 예정)
**배포** 없음 — DB 전용 세션. 런타임 코드 무수정, pm2 ↺36 유지

---

## 0. 세션 시작 프로토콜 (다음 세션은 여기서 시작)

```bash
pm2 ls                                                    # online, ↺ 36 (변동 없음)
curl -so /dev/null -w "%{http_code}\n" localhost:3000     # 200
cd ~/apps/mysado-shop      && git log --oneline -1         # 305792d (+ 47차 커밋 시 추가)
cd ~/apps/mysado-shop-prod && git log --oneline -1         # 6d0610c (세 칸 뒤 — 46차 §7 사유 유효)
```

DB 정합 상태 — **43차 §0 쿼리는 이제 폐기합니다.** 새 기준값:

```sql
-- 주문·로그·장바구니 전부 0, 회원 4, 상품 230 / 재고 합 6489
SELECT (SELECT count(*) FROM orders) o, (SELECT count(*) FROM "user") u,
       (SELECT count(*) FROM product) p, (SELECT sum(stock_quantity) FROM product) s;
```

> `prod-004`=195·`prod-017`=98 (43·45·46차 §0)은 **더 이상 정합값이 아닙니다.** 197·100이 정답입니다.

---

## 1. 이번 세션의 핵심 발견 — 이월 목록은 목록이 아니라 표본이었다

| | 문서 기록 | 47차 실측 |
|---|---|---|
| 테스트 주문 | 로드맵 8-9 "30건" / 43·45·46차 이월표 "4건" | **45건** |
| 이월표 포착률 | — | **4/45 = 9%** |

특히 `MYSADO-260805-0001~0005` **5건은 어느 핸드오프에도 없습니다.** 44차가 만든 것으로 보이며,
`0003`(2,320,500원)·`0004`(2,094,400원)의 비현실적 금액이 44차 이월 7-1-3 **"퀵계좌이체 한도 초과
에러"** 테스트의 흔적입니다. 44차가 자기 산출물을 이월표에 기록하지 않았습니다.

> **교훈**: 테스트 데이터는 만든 세션이 기록하지 않으면 다음 세션이 **존재조차 모릅니다.**
> 이월표의 목록을 대상 전체로 삼았다면 41건이 남았을 것입니다.
> 43차 §8-⑴("착수 전 실측이 설계를 바꾼다")이 네 세션 연속 값을 했습니다.

**실결제 미개시 + 외부 회원 등록 0 → 45건 전부 테스트.** 선별 기준 설계가 불필요했습니다.

---

## 2. 확정된 설계 (Chris 결정 3건 + 반론 수용 1건)

| # | 논점 | 결정 | 근거 |
|---|---|---|---|
| ① | 정리 형태 | **(A) 물리 삭제** | 상태 정리(B)는 매출 집계 오염 지속, 표식 컬럼(C)은 런타임 변경 동반 |
| ② | 규약 충돌 | `DELETE 없음`을 **스키마·마이그레이션 층위로 한정** 재해석 | 원문 맥락이 `DROP/TRUNCATE/ALTER COLUMN` 옆자리 = 스키마 파괴 방지 |
| ③ | 회원 범위 | **4명 삭제 / 4명 유지** | 전량 삭제 시 `account` CASCADE로 재로그인 불가. E2E용 일반회원 1개 필수 |
| ④ | 재고 초기화 | **기각** — 4개 값만 복원 | §2-2 |

### 2-1. 규약 재해석의 근거

정본 규약 `DELETE 없음. 추가 전용(additive-only)`은 `migrate reset` 금지·`--create-only` SQL
육안검사와 같은 문단에 있습니다. 즉 **스키마 파괴 방지 규약**이며, 운영 데이터 행 제거는
사정거리 밖입니다. 다만 규약을 조용히 넓히면 다음 세션이 근거 없이 DELETE를 쓰게 되므로,
**대체 안전장치를 명문화**했습니다(§7).

### 2-2. 🔴 "재고 0 초기화" 반론 — 수용됨

Chris가 "재고를 0으로 초기화해도 무방"이라 하셨으나 반론을 드렸고 채택됐습니다.

- `stock_quantity`는 테스트 산물이 아니라 **7월 스마트스토어 임포트가 가져온 실재고 원본**입니다.
- 이번 세션이 오염시킨 값은 **딱 4개**(`prod-004` −2, `prod-017` −2). 나머지 226개는 한 번도
  변경된 적이 없습니다(43차 실측: `decrement` 코드 0건).
- 0 초기화 시 230개 상품 전량 품절 + **복구 근거가 DB 어디에도 없어 전량 수기 재입력**.

→ **`prod-004` +2 → 197, `prod-017` +2 → 100 만 복원.** 43차 §4 검증 시작값(`prod-004`=197)과
독립적으로 일치하여 두 방향 실측이 같은 숫자를 가리켰습니다.

---

## 3. 코호트 판정 — 차감 여부는 데이터에 남아 있었다

43차 §2-1이 "차감 여부 기록 컬럼 불필요"를 설계로 채택했기 때문에 판정 수단이 없다고
예상했으나, **`paid_at` + `status` 조합이 그 역할을 했습니다.**

| 군 | 건수 | 차감 | 삭제 시 조치 |
|---|---|---|---|
| PENDING | 24 | ❌ | 없음 |
| CANCELED (`paid_at` 有) | 8 | 차감 후 복원 = net 0 | 없음 |
| CANCELED (`paid_at` NULL) | 2 | ❌ (승인 자체 없음) | 없음 |
| DONE `260717-0022` | 1 | ❌ (43차 이전) | 없음 |
| PAID 43차 **이전** 3건 | 3 | ❌ | **없음** — 손대면 유령 재고 |
| PAID 43차 **이후** 2건 | 2 | ✅ | 🔴 **+2 / +2 복원** |

`CANCELED` 두 유형이 `paid_at`으로 정확히 갈립니다. `260804-0004`·`0006`이 43차 §4 시나리오
②·⑥(재고 부족 자동취소 / 전량 롤백)이며, `payment_log`의 `CONFIRM_FAIL` + `STOCK_SHORTAGE`
**2건**이 이를 확증했습니다.

**기준 시각은 `created_at`이 아니라 `paid_at`** — `confirm/route.ts:115·129`가 토스 `approvedAt`을
`paid_at`으로 저장하며 `status: "PAID"`와 같은 update에 둡니다. 주문 생성은 차감하지 않습니다.

### 3-1. 43차 §5-⑶ 위험이 방식 선택으로 소멸했다

두 세션 연속 🔴로 이월된 **"미차감 PAID 3건에 cancel API 호출 금지"** 가 해소됐습니다.
물리 삭제는 cancel 경로를 거치지 않으므로 복원 로직이 개입할 여지가 없습니다.
검증 ④(`prod-003` 99 · `prod-038` 5 · `prod-081` 8 · `prod-199` 470 불변)가 직접 증거입니다.

---

## 4. 실행 내역

### 4-1. 백업

```
~/db-backups/mysado_db-2026-08-08-pre-47cleanup.sql   719K / 15테이블
생성 시각 8/9 00:48 (KST 자정 넘김 — 파일명 8-08과 불일치, 내용 영향 없음)
내용 검증  COPY public.orders 이하 45행 실재 확인
```

### 4-2. 단일 트랜잭션 (`psql -1 -f`)

```
DO            ← 가드 통과 = 조사~실행 사이 DB 무변동 증명
UPDATE 1      prod-004  195 → 197
UPDATE 1      prod-017   98 → 100
DELETE 45     orders  (order_item 110 · payment_log 28 CASCADE 동반)
DELETE 2      cart_item
DELETE 17     search_log
DELETE 4      user    (account 4 · address 1 · session CASCADE 동반)
```

선두 가드 블록이 42차 V5(`expectedCurrent` 대조)의 SQL판입니다. 실측값이 195/98/45와 다르면
`RAISE EXCEPTION`으로 트랜잭션 전체가 롤백됩니다. **자동 보정하지 않습니다.**

### 4-3. 삭제된 회원 / 유지된 회원

```
삭제  이순신 test1@mysado.net (주문16·배송지1·장바구니1)
      김삼성 samsung@mysado.net (전무)
      차민수 cha_02@mysado.net (45차 픽스처)
      탈퇴회원 deleted-HQinn…@deleted.invalid (29차 마스킹 산물, account 0)

유지  홍길동 chris.hynix@gmail.com  admin  (배송지 4)
      우승문 smwoo529@naver.com     admin
      김영웅 best1top@naver.com     user   (배송지 1)
      차민규 cha_01@mysado.net      user   ← 오픈 후 일반회원 E2E용
```

차민규를 남긴 이유: 오픈 후 관리자가 아닌 시점의 검증에 계정이 필요하고, 그때 새로 만들면
**그 계정이 다시 테스트 데이터가 됩니다.**

---

## 5. 검증 기록

### 5-1. 독립 교차검증 8/8

| # | 검증 | 결과 |
|---|---|---|
| ① | 삭제 5종 | orders 0 / order_item 0 / payment_log 0 / cart_item 0 / search_log 0 ✅ |
| ② | **CASCADE 실동작** | 110·28 전량 소멸 — psql 미출력분을 기준선 대조로 확인 ✅ |
| ③ | 재고 복원 | prod-004 **197** / prod-017 **100** ✅ |
| ④ | **미차감분 불변** | prod-003 99 · prod-038 5 · prod-081 8 · prod-199 470 ✅ |
| ⑤ | 회원 | 4명, 명단 일치 ✅ |
| ⑥ | address | 6 → 5 (이순신 1건만 소멸) ✅ |
| ⑦ | account | 8 → 4 ✅ |
| ⑧ | product 무손상 | **230건 / 재고 합 6,489** ✅ |

`session 9`는 잔여 4명의 다중 로그인 세션. 만료 시 자연 정리.

### 5-2. 소멸 전 보존한 증거

`payment_log` 28건이 CASCADE로 사라지므로 분포를 기록합니다(43차 6/6 검증의 원증거).

```
CONFIRM_SUCCESS                 14
CANCEL                          10
CONFIRM_FAIL / STOCK_SHORTAGE    2   ← 43차 시나리오 ②·⑥
WEBHOOK                          2
```

`search_log` 17건 검색어(전량 Chris 테스트 타이핑, 9-4에 쓸 실사용 실패 기록 없음):

```
with / WITH (대소문자 테스트)  seeco / seEco  케이스  갤럭시  스트랩  밴드  스마트
아이폰(0건)  스포츠(0건)  S26 러기드 마그넷 케이스 블랙
```

---

## 6. 🔴 47차 신규 함정 3종

### 6-1. `~/output` 은 존재한 적이 없다

45차 §8-⑷가 "지시서 산출물은 `~/output`으로 옮긴다"를 원칙으로 기록했고 45·46차 산출물 표가
그 경로를 적었으나, **디렉터리 자체가 없습니다.** 38차부터 실제 관례는 계속
`~/apps/mysado-shop/outputs/`(gitignore) 였습니다.

```
ls -d ~/output                       → No such file or directory
ls ~/apps/mysado-shop/outputs        → 38·39·40·42·43·44·45·46차 지시서 10개 실재
```

> 43차 §8-⑸("문서에 상태를 적으면 낡는다")의 변종 — 이번엔 낡은 게 아니라 **처음부터 틀린 기록**이
> 두 세션 전파됐습니다. **산출물 표에 경로를 적을 때 `ls`로 확인**해야 합니다.
> Chris가 지적하지 않았다면 47차도 같은 오기를 이었을 것입니다.

### 6-2. grep 앵커 오류가 검증자 쪽에서 났다

백업 유효성 확인에 `grep -c "^MYSADO"`를 썼고 **0건**이 나왔습니다. `COPY public.orders`의 첫
컬럼은 `id`이고 `order_number`는 15번째라 행 머리에 `MYSADO`가 올 수 없습니다.

44차 §5-2("0건은 '없다'가 아니라 '패턴 오류'일 수 있다")를 **네 세션 연속**, 이번엔 지시하는
쪽이 밟았습니다. `awk`로 COPY 블록 행수를 세어 45행을 확인하고 통과했습니다.

### 6-3. `search_log` 는 일반 컬럼도 camelCase

```
ERROR: column "result_count" does not exist
HINT:  Perhaps you meant to reference the column "search_log.resultCount".
```

정본 규약은 `search_log`의 **FK가 `"userId"`** 라는 것까지만 적고 있었습니다. 일반 컬럼
`"resultCount"`도 camelCase입니다. **`product_view_log`는 미확인** — 다음 접근 시 `\d` 필수.

---

## 7. 신설 규약 (CLAUDE.md 반영 대상)

```
[데이터 삭제] (47차 신설)
"DELETE 없음. 추가 전용(additive-only)"은 스키마·마이그레이션 층위의 규약이다.
운영 데이터 행의 삭제는 이 규약의 대상이 아니며, 대신 아래 5단계가 강제된다.
  1. pg_dump 백업 + 백업 내용 실측(대상 테이블 행수 확인)
  2. 삭제 대상과 CASCADE 자식 행 수를 SELECT로 사전 확정
  3. 사람 검수 (cat -n)
  4. 단일 트랜잭션(psql -1 -f) + 선두 가드 블록(기대값 불일치 시 RAISE EXCEPTION)
  5. 독립 재조회 교차검증
마이그레이션 SQL의 DROP/TRUNCATE/ALTER COLUMN/DELETE 금지는 그대로 유효하다.

[CASCADE 함정] FK ON DELETE CASCADE는 자식 삭제 건수를 출력하지 않는다.
삭제 전 자식 행 수를 세어 두지 않으면 "무엇이 사라졌는지 모르는 삭제"가 된다.

[산출물 경로] 지시서·SQL·리포트는 ~/apps/mysado-shop/outputs/ (gitignore).
~/output 은 존재하지 않는다(45차 기록 오류).

[컬럼명] search_log 는 FK "userId" 뿐 아니라 일반 컬럼 "resultCount" 도 camelCase.
어떤 경우에도 추측 금지 — \d "테이블명" 선행.
```

식별자·실명이 든 `47차-cleanup.sql`은 **커밋하지 않습니다**(gitignore 유지). 감사 근거는
백업·이 문서·대화 로그 세 겹으로 충분하며, git 이력은 사후 삭제가 어렵습니다.

---

## 8. 이월 항목

### 8-1. 신규 (47차 발생)

| # | 항목 | 비고 |
|---|---|---|
| 1 | **`product_view_log` 0건** | 조회 로깅이 작동하지 않거나 비회원 미기록. 9-5 랭킹의 입력이므로 오픈 전 원인 확인 필요 |
| 2 | `~/output` 오기 3곳 | 45차 §8-⑷·§9, 46차 §10. 문서 정정 대상 |
| 3 | CLAUDE.md 규약 4종 추가 | §7 |
| 4 | 43차 §0 재고 쿼리 폐기 | 195/98 → 197/100. 46차까지 세 세션이 옛 값을 복사 |
| 5 | `product_view_log` 컬럼 규칙 미확인 | `search_log`와 같은 계열일 가능성 |

### 8-2. 해소된 항목

- **8-9 테스트 데이터 정리 → 완료** ✅ (28차 관찰 이후 6세션 만)
- **🔴 43차 §5-⑶ 미차감 PAID 3건 cancel 금지 → 위험 소멸** ✅ (§3-1)
- 테스트 주문 `MYSADO-260804-0001·0002·0005·0007` → 45건 전체와 함께 삭제 ✅

### 8-3. 기존 이월 (46차에서 승계)

- 게이트 승격 write 미구현 / 두 축(`content_status` ↔ 노출) 연결 미정
- **Layer 3b 잔여 188건** — 게이트로 규모 확정. 병목은 이미지 준비(Chris 수동)
- 장바구니 수량 직접 입력(44차 7-1-2) / 퀵계좌이체 한도 초과 에러 → 8-3 점검
- 웹훅 두 경로 미검증(43차 §4) / Prisma 트랜잭션 타임아웃 5초(43차 §5-⑵)
- 라이브 전환 후 계좌이체 취소 시 `refundReceiveAccount` 요구 가능성
- psql 배치 금지(46차 §6) / 커밋 메시지 `!` 금지 / 국제번호 비일관
- `app/api/mypage/addresses/*` raw SQL 3건 / `01_payment.md` 결제창 방식 기준
- `product.status` DEFAULT `'SALE'` 구값 / `layer3b-write.js` V6(G5가 탐지망 대체)
- prod-011 `로열핑/뱅` / prod-154 `model_name` / `lasertTransparent` 관례
- 42차 절단 18건(`라이트` 10·`다크` 8) / `라이트블루` 3건 / `prod-066` 투명
- 미조사 사각 ⓒ 106건(46차 WARN 계측) / ⓐⓑ 미조사
- mysado.co.kr 301 / 소셜로그인 / `admin-guard.ts` 타입 캐스트

---

## 9. 🔴 오픈 게이트 — 일정 유연화에 따른 재설계 제안

Chris 결정: **8/16을 유연하게, 완성도 우선.** 이에 따라 **두 날짜를 분리**할 것을 제안합니다.

- **토스 라이브 계약 완료** — 외부 일정. 서류 보완 재심사 중. 승인되면 키를 받아 둡니다.
- **쇼핑몰 실오픈** — 우리가 정합니다. **키를 받는 것과 여는 것은 별개입니다.**

승인 직후 8-3(키 교체·폴백 제거·010 가드)을 처리해 *라이브 결제가 가능한 상태*로 만들어 두되,
오픈은 아래 4건이 닫힐 때 합니다.

| # | 조건 | 상태 | 성격 |
|---|---|---|---|
| ① | **재고 실물 대조** | 🔴 미착수·미인지 | 7월 임포트 후 타채널 판매분 미반영. **초과판매 직결** |
| ② | **오프사이트 백업(N-1)** | 🔴 미착수 | 백업 전부 같은 미니PC 안. 실주문 데이터 전손 위험 |
| ③ | **다운타임 알림(N-2)** | 🔴 미착수 | 홈서버 정전·크래시를 아무도 알려주지 않음 |
| ④ | 라이브 웹훅 실발신 수신 | ⚠️ 승인 후에만 가능 | 유일한 미검증 결제 경로 |

**Layer 3b 188건은 오픈 게이트가 아닙니다.** 46차가 `content_status`와 노출이 무관함을
실측했으므로 열어 둔 채로 계속 채울 수 있고, 게이트에 넣으면 이미지 준비 병목으로 오픈이
무기한 밀립니다. 위 4건은 성격이 다릅니다 — **열린 뒤에 발견하면 돈과 신뢰가 걸립니다.**

①이 가장 시급합니다. 이번 세션이 확정한 **재고 합 6,489**가 그 대조의 출발점입니다.

---

## 10. 새로 확립된 원칙

**⑴ 이월 목록은 대상 전체가 아니다** — 45건 중 이월표 포착은 4건(9%). 정리 세션은
목록이 아니라 **전수 조회**에서 출발한다(§1).

**⑵ 삭제는 원장 재계산을 동반한다** — 행이 사라지면 복원 근거도 함께 사라진다.
**수량 합을 먼저 확보한 뒤 삭제**한다. 순서가 뒤바뀌면 복구 불능(43차 §8-⑶의 삭제판).

**⑶ 방식 선택이 위험을 없앨 수 있다** — 43차 §5-⑶는 두 세션 이월된 🔴였으나,
cancel 대신 물리 삭제를 택하자 **회피가 아니라 소멸**했다(§3-1).

**⑷ 산출물 경로는 문서가 아니라 파일 시스템이 정본** — `~/output`은 두 세션 문서에
기록됐으나 존재한 적이 없다. 경로를 적을 땐 `ls`로 확인한다(§6-1).

**⑸ 규약을 넓힐 땐 대체 안전장치를 함께 명문화한다** — `DELETE 없음`을 좁게 재해석하는 대신
5단계 절차를 신설했다. 재해석만 하고 절차를 안 만들면 다음 세션이 근거 없이 DELETE를 쓴다(§7).

---

## 11. 산출물

| 경로 | 내용 |
|---|---|
| `~/db-backups/mysado_db-2026-08-08-pre-47cleanup.sql` | 719K / 15테이블 (8/9 00:48 생성) |
| `~/apps/mysado-shop/outputs/47차-cleanup.sql` | 실행 SQL 36줄, gitignore (커밋 안 함) |
| 이 문서 | 47차 핸드오프 |

DB 전용 세션 — **런타임 코드 무수정, 빌드·재시작 없음**(42·46차형). 마이그레이션 0건.

---

## 12. 48차 착수 권고

**A. 재고 실물 대조 (오픈 게이트 ①)** — 최우선. 임포트 후 3주간 스마트스토어·타채널
판매분이 mysado.net에 반영된 적이 없습니다. 230건 전수 대조는 수작업이므로 방법 설계부터
필요합니다(전수 vs 재고 소량 상품 우선 vs 채널 데이터 export 대조).

**B. 오프사이트 백업 + 다운타임 알림 (게이트 ②③)** — 합쳐서 반나절 분량.
rclone 야간 cron + 외부 uptime 모니터. **분기 복구 리허설**까지가 한 세트.

**C. CLAUDE.md 규약 반영 + 문서 정정** — §7 4종 + §8-1 항목 2·4. 30분 분량, 아무 세션 서두.

> **마감**: Toss 라이브 계약 — **서류 보완 재심사 중**. 8/16 기한의 구속력이 약해졌고
> Chris 결정으로 완성도 우선. 승인 시 8-3 묶음이 최우선이며, 그 세션에 46차 커밋 2개 +
> 47차 문서가 prod로 동반됩니다(현재 격차 3칸).
