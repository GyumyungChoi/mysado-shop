# Phase 7 — 44차 핸드오프

**일자** 2026-08-05
**주제** 결제 UX 묶음 (43차 이월 1·2·3) — 연락처·우편번호 검증 공용화 · 정규화 통일
**커밋** `a133b57`(refactor) → `1e354bf`(fix)
**배포** prod `1e354bf` 반영 완료 (pm2 ↺ 35)

---

## 0. 세션 시작 프로토콜 (다음 세션은 여기서 시작)

```bash
pm2 ls                                                    # online, ↺ 35
curl -so /dev/null -w "%{http_code}\n" localhost:3000     # 200
cd ~/apps/mysado-shop      && git log --oneline -1         # 1e354bf
cd ~/apps/mysado-shop-prod && git log --oneline -1         # 1e354bf (일치)
```

> **43차 §0의 기대값이 이미 틀려 있었습니다.** `a271169`로 적혀 있었으나 실제 dev는
> `d0da87f`(핸드오프 문서 커밋)였습니다. 핸드오프 문서를 커밋하는 순간 그 문서에 적힌
> 커밋 해시가 낡습니다. **프로토콜의 기대값은 "문서 커밋 이후"를 기준으로 적어야 합니다.**
> 이 문서의 `1e354bf`도 같은 이유로, 이 핸드오프 커밋 후에는 한 칸 뒤가 됩니다.

DB 무접근 세션이었습니다. 43차 §0의 재고 정합 쿼리는 그대로 유효합니다
(`prod-004` = 195, `prod-017` = 98 — 세션 시작·종료 시 변동 없음 확인).

---

## 1. 세션 성격 — 실측이 세 가지 전제를 뒤집었다

43차 이월 1·2·3은 문서상 "UX 개선 3건"이었습니다. 착수 전 실측에서 셋 다 성격이 달랐습니다.
43차 §8-⑴("착수 전 실측이 설계를 바꾼다")이 두 세션 연속으로 적중했습니다.

| | 43차 기록 | 44차 실측 |
|---|---|---|
| 이월 1 | 연락처 400 **UX** 문제 | **정규화 불일치 버그** — 유효한 입력이 400을 받고 있었음 |
| 이월 2 | 재고 표시 (데이터 정확도 문제) | **순수 표시 작업** — 데이터는 이미 클라이언트에 있음 |
| 이월 3 | 정규식 복제 **2곳** | **"정규화 → 검증" 쌍이 2벌** + `formatPhone` 4번째 사본 |

### 1-1. 이월 1의 진짜 정체 — 정규화가 두 종류였다

```
lib/format-phone.ts:19        replace(/[^0-9]/g, "")   ← 표시용, 숫자만 남김
lib/orders.ts:19              replace(/[-\s]/g, "")    ← 검증용, 하이픈·공백만
lib/address-validation.ts:27  동일
```

**같은 값이 표시는 되는데 검증은 탈락하는 구조였습니다.**

- `010.1234.5678` → 표시 정상, 검증 400 (점이 안 지워져 `^0\d{8,10}$` 탈락)
- 자동완성이 채운 `+54…` → `+`가 남아 `^0`에서 탈락

43차가 "브라우저 자동완성이 `+54…`를 채웠고 에러가 화면 최상단이라 미인지"로 기록한 것은
**증상 두 개 중 뒤엣것만** 본 것이었습니다. 스크롤을 고쳐도 400 자체는 계속 났을 것입니다.

**정규화를 `[^0-9]`로 통일한 것이 이번 세션의 실제 수정이고, 인라인 검증·스크롤은 그 위의 층입니다.**

### 1-2. 이월 3의 규모 — 함수로 빼야 했던 이유

| 대상 | `lib/orders.ts` | `lib/address-validation.ts` |
|---|---|---|
| 정규화 | 19행 | 27행 |
| 전화 `/^0\d{8,10}$/` | 20행 | 28행 |
| 우편번호 `/^\d{5}$/` | 25행 | 33행 |

정규식만 상수로 뺐다면 **정규화는 여전히 2벌로 남아** 1-1의 버그가 그대로였습니다.
43차 이월 항목의 "동일 정규식 상수화"라는 표현이 해법을 좁게 규정하고 있었습니다.

### 1-3. 이월 2 — 침묵 반환은 데이터 문제가 아니었다

`app/cart/page.tsx`는 `"use client"`이고 `item.stock`을 이미 보유합니다.
74행 `if (newQuantity < 1 || newQuantity > item.stock) return;`이 아무 안내 없이 반환하고 있었습니다.
**서버·API 변경 0건**으로 끝나는 작업이었습니다.

추가 실측으로 **`+` 버튼이 이미 `disabled`**(219행 `item.quantity >= item.stock`)임이 드러나,
새로 넣은 가드는 도달 불가 코드가 됩니다. 그래서 가드는 방어용으로만 남기고
**한도 도달 자체를 표시**하는 쪽으로 해법을 바꿨습니다.

---

## 2. 확정된 설계

### 2-1. 두 경로의 규칙을 분리한다

| 경로 | 규칙 | 근거 |
|---|---|---|
| 배송지 `recipientPhone` | `^0\d{8,10}$` — **유선 허용** | 수취인이 유선번호일 수 있음 |
| 회원 `phone_number` | `^01[016789]\d{7,8}$` — **휴대폰 전용** | 본인 식별·향후 SMS 인증 |

한 상수로 합치면 8-3의 `customerMobilePhone` 010 가드와 충돌합니다.
`lib/validation/contact.ts`에 **이름을 달리해 둘 다 정의**하되, 이번 세션에 회원 경로는 수정하지 않았습니다.
`isValidMemberPhone`은 **정의만 있고 호출부가 없는 상태**입니다 — 8-3이 재사용할 예정입니다.

### 2-2. 회원 연락처 UNIQUE는 이번에 넣지 않았다

Chris 요청은 "회원 유일성 및 검증"이었으나 둘은 다른 작업입니다.

- **검증**(휴대폰 전용 규칙) — 정의만 완료
- **UNIQUE 제약** — 🔴 스키마 마이그레이션. 로드맵 **10-6**(SMS 본인인증 + phoneNumber unique) 소속

전화번호는 통신사가 회수 후 재발급하는 **재사용 식별자**입니다. 본인인증 없이 UNIQUE만 걸면
"타인 번호를 먼저 등록해 그 사람의 가입을 막는" 경로가 열립니다.
10-6이 SMS 인증과 UNIQUE를 한 묶음으로 둔 이유가 이것입니다.

**실측 — 지금 걸어도 충돌은 없습니다** (참고용, 착수 근거 아님):

```
전체 6 / 값보유 5 (NULL 1) / 고유 5 / 규칙위반 0
```

이미 전부 `01X` 11자리 정규형입니다. 44차의 검증 규칙이 호출부에 붙으면 이 상태가 유지되므로,
**10-6 착수 시 선행 데이터 정리가 불필요**해집니다. 그 준비로서 순서가 맞습니다.

### 2-3. `showError` 래퍼 — 8곳에 뿌리지 않는다

`setErrorMessage` 호출이 **9곳**, 렌더는 화면 최상단 **한 곳**뿐이었습니다.
각 호출부에 `scrollTo`를 붙이면 복제가 9개 생깁니다. 래퍼 하나로 감싸고 전 호출을 교체했습니다.

```js
const showError = useCallback((message: string | null) => {
  setErrorMessage(message);
  if (message) window.scrollTo({ top: 0, behavior: "smooth" });
}, []);
```

`null`(에러 해제)일 때 스크롤하지 않으므로 해제 경로도 그대로 교체 가능합니다.
기존 307행에 있던 개별 `scrollTo`는 중복이 되어 제거했습니다(스크롤 2회 트리거 방지).

### 2-4. `onBlur`만으로는 43차 사고가 재현된다 🔴

**브라우저 자동완성은 `blur`를 발화하지 않는 경우가 많습니다.** 사용자가 필드를 탭으로 빠져나가지
않으면 인라인 에러가 뜨지 않고 그대로 제출 → 서버 400 → 상단 스크롤로 돌아갑니다.

제출 직전 가드를 추가해야 닫힙니다. `showError`는 부르지 않고 **필드에 `focus()`** 합니다 —
포커스가 스크롤을 데려가므로 상단 배너보다 정확한 위치로 가고, 두 스크롤이 경합하지 않습니다.

### 2-5. 상세 페이지에 재고 숫자를 노출하지 않는다

`app/products/[id]/page.tsx:48` → `export const revalidate = 60` (ISR).
**43차 기술이 맞고 34차 §1-4의 "force-dynamic"은 이 페이지에 해당하지 않습니다.**
빌드 출력에서 `ƒ (Dynamic)`으로 표시되지만 이는 동적 파라미터라 요청 시 렌더 후 60초 캐시된다는
뜻이며, 최대 60초 낡은 값이 보인다는 판정은 그대로입니다.

장바구니에서도 **숫자를 노출하지 않습니다** — `"N개 남음"` 금지, `"재고 한도에 도달했습니다."`만
(판매량 추정 단서 차단, Chris 결정).

---

## 3. 변경 내역

```
lib/validation/contact.ts           신규 63줄 — 연락처·우편번호 규칙의 유일한 정본
lib/orders.ts                       정규화·검증·문구를 함수·상수 호출로 대체
lib/address-validation.ts           동일
app/checkout/page.tsx               showError 래퍼 / phoneError 상태 / ref+focus 제출 가드
                                    / onBlur 검증 / autoComplete·inputMode / aria 속성
app/mypage/addresses/AddressList.tsx  onBlur 검증 (scrollTo 없음 — §4-③)
app/cart/page.tsx                   침묵 반환 분리 + 한도 도달 표시
prisma/schema.prisma                무수정 — 마이그레이션 0건
```

**`lib/validation/contact.ts`는 Prisma·서버 모듈을 import하지 않습니다.** 클라이언트 컴포넌트가
직접 import하므로 의존이 생기면 번들이 깨집니다. `"use client"`도 붙이지 않은 중립 모듈입니다.

**에러 문구는 서버 400 응답과 폼 인라인 에러가 같은 상수를 씁니다.**

```
RECIPIENT_PHONE_ERROR = "연락처를 다시 확인해주세요. 010-1234-5678 또는 02-123-4567 형식으로 입력해주세요."
MEMBER_PHONE_ERROR    = "휴대폰 번호를 다시 확인해주세요. 010으로 시작하는 번호를 입력해주세요."
ZIP_ERROR             = "우편번호를 다시 확인해주세요. 5자리 숫자여야 합니다."
```

`RECIPIENT_PHONE_ERROR`에 **02 예시를 함께 든 것은 유선 허용 결정의 결과**입니다.
010만 보이면 유선 수취인이 못 쓴다고 오해합니다.

---

## 4. Claude Code 산출물 평가 — 지시서보다 정확했던 2건

42차에 이어 지시서 준수도가 높았고, **지시가 틀린 곳을 두 번 잡아냈습니다.**

**⑴ `!selected` — 채택 (지시서가 틀림)**

지시서는 "직접입력 모드일 때만"을 `selectedAddressId === null`로 쓰라고 했으나,
`selectedAddressId`가 있는데 `addresses.find`가 실패하는 경우(다른 탭에서 배송지 삭제)
`selected`가 `undefined`가 되어 **폼 state가 전송되는데 검증은 건너뛰는** 구멍이 생깁니다.
`!selected`가 "폼 state를 보내는 경우"와 정확히 일치합니다.

> **교훈**: 조건은 UI 모드가 아니라 **실제로 무엇이 전송되는가**로 써야 합니다.

**⑵ `setIsSubmitting(false)` 명시 해제**

제출 가드가 `try` 이전이라 `finally`가 돌지 않습니다. 놓쳤으면 버튼이 영구 비활성으로 굳었습니다.

**⑶ `normalizeZipCode` 자발 추가 — 승인**

`isValidZipCode`만 `[^0-9]` 정규화하고 저장은 `.trim()`을 유지하면 **검증한 값과 저장한 값이
어긋납니다**. `{"zipCode":"12 345"}`를 API에 직접 보내면 검증 통과 + DB에는 공백 포함 저장.
UI는 `readOnly`라 도달 불가지만 API 직접 호출은 가능합니다(자체 REST API 설계가 있으므로 실재 위험).

**⚠️ 다만 지시서 §4의 "동작 불변" 서술이 이로써 틀렸습니다.** 전화·우편번호 **둘 다 정규화가
관대해졌습니다.** 기존에 400이던 일부 입력이 통과합니다 — 이것이 목적이지만, 리팩터링 커밋에
동작 변경이 섞였다는 사실은 기록해 둡니다.

**⑷ 보류 판단 4건 — 전부 실측 후 보고, 임의 진행 없음**

| 항목 | 실측 | 판정 |
|---|---|---|
| 기존 에러 문구 참조 | 정의부 4곳 외 참조 0, 테스트 파일 자체 부재 | 교체 안전 |
| 우편번호 필드 | 양쪽 UI 모두 `readOnly`, 우편번호 찾기로만 채워짐 | `onBlur` 미적용 |
| `AddressList` 스크롤 | 모달이 아닌 **인라인 폼** — 상단 스크롤 시 보던 폼이 화면 밖으로 | 인라인 에러만 |
| `+` 버튼 `disabled` | 219행에 이미 `item.quantity >= item.stock` | 가드는 방어용, 한도 표시 신설 |

**⑸ 품절 문구 겹침 — 스스로 발견하고 스스로 고쳤으나 보고에 반영 안 함** 🟡

`stock: 0`이면 `0 >= 0`이 성립해 빨간 "품절" 아래 회색 "재고 한도에 도달했습니다."가 겹칩니다.
Claude Code가 이를 보고하며 "지시하신 조건 그대로 두었으니 바꾸려면 한 줄"이라고 적었으나,
**실제 파일에는 이미 `item.isAvailable &&`가 들어가 있었습니다.**

> **교훈 (44차 신규)**: **보고문보다 파일 실측이 우선입니다.** 이번에 Claude가 보고문을 근거로
> "미반영"이라 판단해 불필요한 수정 지시를 냈고, `grep`을 돌리고 나서야 이미 반영됐음이 드러났습니다.
> 42차 §5-2의 "`row.specs` → `specs` 자기 수정"과 같은 계열입니다 — Claude Code는 작업 중
> 자기 판단으로 고치고 보고에 다 반영하지 않는 경우가 있습니다.

---

## 5. 검증 기록

### 5-1. 정적 검사

```
npx tsc --noEmit        출력 없음, exit 0
npx next lint --file    6개 파일 전부 ✔ No ESLint warnings or errors
npm run build (prod)    ✓ Compiled / ✓ Linting and checking validity of types / 37 pages
```

### 5-2. 복제 잔존 전수 확인 (커밋 전)

```
전화 정규식 {8,10}     → lib/validation/contact.ts 1곳만  ✅
우편번호 정규식 d{5}   → lib/validation/contact.ts 1곳만  ✅
구 정규화 [-\s]        → 배송지 경로 0건 ✅ (회원 경로 3곳은 §7 이월)
```

> **이 검사를 처음엔 놓칠 뻔했습니다.** 최초 grep이 `\\\\d`로 이스케이프돼 쉘에서 `\\d`(리터럴
> 백슬래시+d)로 전달되어 **0건이 나왔고**, 그것을 "복제 없음"으로 오독할 뻔했습니다.
> 43차 이월 문서가 "2곳"이라 명시했기에 0건이 이상함을 알아챈 것입니다.
> **grep이 0건일 때 "없다"와 "패턴이 틀렸다"를 구분해야 합니다.**

### 5-3. dev 실검증 8/8 통과

| # | 시나리오 | 결과 |
|---|---|---|
| ① | `010.1234.5678` 제출 | **통과** ✅ (44차 핵심 — 정규화 통일 실증) |
| ② | `+54 9 11 1233-5678` 붙여넣고 **탭 없이** 제출 | 인라인 에러 + 필드 포커스, 400 안 감 ✅ |
| ③ | 잘못된 번호 후 탭 이동 | 필드 아래 인라인 에러, 스크롤 없음 ✅ |
| ④ | 주소 비우고 제출 (서버 400 유발) | `ZIP_ERROR` 상단 표시 + 스크롤 ✅ |
| ⑤ | 저장 배송지 선택 후 제출 | 가드 통과 ✅ |
| ⑥ | `02-123-4567` | **통과** ✅ (유선 허용 실증) |
| ⑦ | 장바구니 한도 도달 | 회색 "재고 한도에 도달했습니다.", 숫자 없음 ✅ |
| ⑧ | 마이페이지 배송지 폼 | 인라인 에러, 스크롤 없음 ✅ |

②가 43차 사고의 직접 재현이며, `onBlur` 단독으로는 막지 못했을 경로입니다(§2-4).

### 5-4. prod 배포·실사이트

```
push        d0da87f..1e354bf   (43차 잔여 a271169·d0da87f 동반 배포)
prod pull   bb32776..1e354bf   create mode lib/validation/contact.ts 확인
build       성공 → pm2 restart → ↺ 35 → pm2 save (dump.pm2 기록 확인)
curl        localhost:3000 200
실사이트     010.1234.5678 통과 / 장바구니 한도 문구 표시  ✅
```

**🟡 curl 교차검증이 불가능한 세션이었습니다.**

```
curl -s https://mysado.net/cart     | grep -c "재고 한도에 도달"   → 0
curl -s https://mysado.net/checkout | grep -o 'tel-national'      → 없음
```

두 페이지는 `"use client"`이고 데이터가 로그인 후 fetch로 채워지므로 초기 HTML에 해당 마크업이
없습니다. **배포 실패의 증거가 아니라 검증 수단이 닿지 않는 영역**입니다.
정본 규약의 "curl로 실제 변경 내용 확인"은 **서버 렌더 페이지에만 적용 가능**하며,
클라이언트 컴포넌트 변경은 브라우저 육안 확인이 유일한 수단입니다.

---

## 6. 이번 세션에서 드러난 함정 2건

### 6-1. `!` 이력 확장이 `psql -c`에서도 발생한다 🔴

42차 §7-1이 `node -e`·히어독 사례로 기록한 문제가 **psql `-c` 경로에서도 동일하게 재현**됐습니다.

```sql
count(*) FILTER (WHERE phone_number !~ '^01[016789]...')
```

bash 이력 확장이 쌍따옴표 안의 `!~`를 이력 참조로 해석해, **직전 명령 중 `~`로 시작하는 것**
(`cd ~/apps/mysado-shop && pwd`)으로 치환했습니다. 두 번 연속 같은 자리에서 깨졌습니다.

**회피 규약 (44차 신설)**: 정규식 부정에 `!~` 대신 **`NOT (... ~ ...)`** 를 씁니다.
`set +H`는 잊어버리기 쉽지만 연산자 선택은 명령 안에 남습니다.

### 6-2. `user` 테이블 컬럼 규칙 — 정본 규약 정정 🔴

정본 규약에 "Better Auth 테이블(user/session/account)은 camelCase"로 적혀 있으나,
`user."phoneNumber"`는 **존재하지 않습니다.** 실제 컬럼은 `phone_number`입니다.

**정확한 표현**:
> Better Auth가 **생성한 컬럼**은 camelCase(쌍따옴표 필요),
> 프로젝트가 **추가한 컬럼**은 snake_case. **한 테이블 안에 두 규칙이 섞입니다.**

43차 §6-2가 CLAUDE.md에 넣은 `stock`↔`stock_quantity` 매핑과 같은 계열의 함정입니다.
**CLAUDE.md·프로젝트 지침에 반영 필요.**

---

## 7. 이월 항목

### 7-1. 신규 (44차 발생)

| # | 항목 | 비고 |
|---|---|---|
| 1 | **회원 연락처 경로 복제 4곳** 🔴 | `app/signup/page.tsx:36` · `app/api/mypage/profile/route.ts:44` · `app/mypage/profile/ProfileForm.tsx:35` (전부 구 정규화 `[-\s]`) + `ProfileForm.tsx:13` 로컬 `formatPhone`(11자리만 처리 — 공용은 9~12자리라 **같은 이름 다른 동작**). **배송지에서 고친 버그가 회원가입에는 그대로 남아 있습니다** — `010.1234.5678`이 가입 시 여전히 탈락. `isValidMemberPhone`이 이미 정의돼 있어 호출부만 붙이면 됨 |
| 2 | **장바구니 수량 직접 입력 불가** | `−`/`+` 클릭만 가능. 재고 195개 항목에서 1로 줄이려면 194번 클릭. 입력 필드 도입 시 검증(음수·0·재고초과·비숫자)+디바운스 PATCH가 따라오고 43차 재고 차감 경로와 맞물림 — **별도 세션 분량** |
| 3 | 퀵계좌이체 한도 초과 에러 | 고액 주문 시 발생. 라이브 전환 후 실제 발생 가능 — **8-3 점검 항목**. 현재 문구가 사용자에게 어떻게 보이는지 확인 필요 |
| 4 | 국제번호 처리 비일관 🟡 | `+82 10-1234-5678` → 12자리로 **탈락** / `+05...`처럼 우연히 `0`으로 시작하면 **통과**. 44차 범위 밖으로 의도적 제외. 규칙을 손대면 검증을 처음부터 다시 해야 함 |
| 5 | 품절 문구 겹침 **미검증** | 조건이 `item.isAvailable &&`로 배타이므로 논리상 안 겹치나, `stock:0` 항목을 장바구니에 담는 재현이 어려워 육안 확인 못 함. 43차 §8-⑵대로 "검증했다"고 적지 않음 |
| 6 | `ZIP_ERROR` 뉘앙스 손실 | 기존 `"우편번호는 우편번호 찾기로 입력해주세요"`는 **조작 안내**였으나 새 문구는 형식 설명. 양쪽 UI 모두 `readOnly`라 사용자 도달 경로가 API 직접 호출뿐이어서 실질 영향 없음 |
| 7 | 정본 규약 정정 필요 | §6-1 `!~` 회피 / §6-2 `user` 컬럼 규칙 / curl 검증 한계(§5-4) — CLAUDE.md 반영 |

### 7-2. 기존 이월 (43차에서 승계)

- 연락처 정규식 상수화 → **44차 완료** (배송지 경로만, 회원 경로는 7-1-1)
- 연락처 400 UX → **44차 완료**
- 재고 표시 → **44차 완료**
- 웹훅 두 경로 미검증 (43차 §4) / Prisma 트랜잭션 타임아웃 5초 (43차 §5-⑵)
- 라이브 전환 후 계좌이체 취소 시 `refundReceiveAccount` 요구 가능성
- 테스트 주문 정리 — `MYSADO-260804-0001·0002`(PENDING), `0005·0007`(PAID) → **8-9**
- **🔴 기존 `PAID` 주문 미차감분 — `cancel` API 호출 금지** (43차 §5-⑶)
- `app/api/mypage/addresses/*` raw SQL 3건 / `01_payment.md` 결제창 방식 기준
- `product.status` DEFAULT `'SALE'` / `layer3b-write.js` V6 전역 유일성 검사
- prod-011 `로열핑/뱅` 오타 / prod-154 `model_name` / `lasertTransparent` 관례
- 42차 절단 18건(`라이트` 10 · `다크` 8) / `라이트블루` 3건 / `prod-066` 투명
- 7-3 게시 게이트(draft→review→published) 미구현
- mysado.co.kr 301 / 소셜로그인 / `admin-guard.ts` 타입 캐스트

### 7-3. 🔴 토스 라이브 (기한 2026-08-16)

| 항목 | 상태 |
|---|---|
| 라이브 계약 신청 | ✅ **신청 완료, 심사 중** (44차 시점) |
| footer 사업자정보 / 배송·교환·반품 정책 | ✅ 완료 |
| 8-2 조건부 재고 차감 | ✅ 43차 완료 |
| 라이브 웹훅 실발신 수신 확인 | ⚠️ 심사 통과 전 검증 불가 |
| 키 교체 → prod 재빌드 (`NEXT_PUBLIC_*` 빌드타임 baked) | 계약 승인 직후 |
| 전환 폴백 cuid 분기 제거 / `customerMobilePhone` 010 가드 / 채번 동시성 재시도 | 계약 승인 직후. **010 가드는 `isValidMemberPhone` 재사용** |

---

## 8. 새로 확립된 원칙

**⑴ 보고문보다 파일 실측이 우선이다** — Claude Code는 작업 중 자기 판단으로 고치고 보고에
반영하지 않는 경우가 있습니다(§4-⑸). 보고를 근거로 판단하면 이미 된 일을 다시 지시하게 됩니다.
42차 §5-2의 자기 수정 사례와 같은 계열입니다.

**⑵ grep 0건은 "없다"가 아닐 수 있다** — 이스케이프 오류로 0건이 나온 것을 "복제 없음"으로
오독할 뻔했습니다(§5-2). 0건이 예상과 다르면 **패턴을 먼저 의심**합니다.

**⑶ 조건은 UI 모드가 아니라 전송 내용으로 쓴다** — `selectedAddressId === null`이 아니라
`!selected`(§4-⑴). 모드 플래그와 실제 데이터 흐름이 어긋나는 경로가 존재합니다.

**⑷ 이월 항목의 문구가 해법을 좁힌다** — "동일 정규식 상수화"라는 표현이 정규화 통일이라는
진짜 해법을 가리고 있었습니다(§1-2). **이월을 적을 때 증상이 아니라 관찰 사실을 적습니다.**

**⑸ 검증 수단에도 적용 범위가 있다** — curl 교차검증은 서버 렌더 페이지에만 유효하며,
클라이언트 컴포넌트 변경에는 닿지 않습니다(§5-4). 규약을 기계적으로 적용하고 "확인했다"고
적으면 안 됩니다.

**⑹ 핸드오프의 커밋 해시는 문서 커밋 이후 기준으로 적는다** — 43차 §0의 기대값이 이미 틀려
있었습니다(§0). 문서를 커밋하는 순간 그 문서에 적힌 해시가 낡습니다.

---

## 9. 산출물

| 경로 | 내용 |
|---|---|
| `lib/validation/contact.ts` | 63줄, 신규 (커밋 `a133b57`) |
| `lib/orders.ts` · `lib/address-validation.ts` | 검증부 대체 (커밋 `a133b57`) |
| `app/checkout/page.tsx` · `AddressList.tsx` · `app/cart/page.tsx` | UX (커밋 `1e354bf`) |
| `outputs/작업지시서-44차-결제UX묶음.md` | Claude Code 지시서 (gitignore) |

DB 무접근 — **백업 없음**(42·43차의 `pg_dump` 의례가 적용되지 않는 세션. 되돌리기는 git으로 충분).
마이그레이션 0건.

---

## 10. 45차 착수 권고

**A. 회원 연락처 경로 통일 (7-1-1)** — 44차와 같은 작업의 나머지 절반이고,
**같은 버그가 가입 폼에 그대로 살아 있습니다.** `isValidMemberPhone`이 이미 정의돼 있어
호출부만 붙이면 되므로 가장 싼 항목입니다. `ProfileForm`의 로컬 `formatPhone` 제거도 함께.

**B. 7-3 게시 게이트** — 로드맵 미착수. **판정기 + 리포트로 먼저 만들 것을 권합니다.**
게이트 조건에 `seoTitle`·`seoDescription`이 들어가는데 Layer 3b가 비어 있어, 게이트만 켜면
통과 건수가 사실상 0입니다. 리포트 형태면 "몇 건이 무엇 때문에 탈락하는가"가 숫자로 나오고,
그것이 **Layer 3b 부채 규모의 첫 계측**이자 배치 우선순위 근거가 됩니다.

**C. 8-9 테스트 데이터 정리** — 실결제 개시 직전이 적기입니다. 지금 하면 이후 검증에서 다시
쌓입니다. 43차 §5-⑶(미차감 주문)과 순서 설계가 필요하고, **코호트가 둘로 갈립니다**
(43차 이전 = 미차감, 이후 = 차감됨).

**D. 장바구니 수량 직접 입력 (7-1-2)** — 실사용 결함이나 독립 세션 분량.

> **마감 있는 항목**: Toss Payments 라이브 계약 2026-08-16 — **심사 중**.
> 심사 통과 시 8-3 묶음이 최우선이 되므로, 그 전까지 가벼운 항목(A)을 처리하는 것이 효율적입니다.
