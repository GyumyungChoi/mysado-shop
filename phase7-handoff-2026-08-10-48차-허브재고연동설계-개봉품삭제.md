# Phase 7 — 48차 핸드오프

**일자** 2026-08-10
**주제** 재고 실물 대조 → **허브 재고 연동 설계로 전환** · 단순개봉품 2건 삭제 · 매핑 축 확정 · 미등록 10건 식별
**DB write** `product` 2행 삭제 (230 → 228)
**코드 변경** 0 — 빌드·재시작 없음 (pm2 ↺ 36 유지)
**백업** `~/db-backups/mysado_db-2026-08-10-pre-48delete.sql` (650K, 15테이블, product 230 실측)

---

## 0. 세션 시작 프로토콜 (49차는 여기서)

```bash
pm2 ls                                                    # online, ↺ 36 (48차 재시작 없음)
curl -so /dev/null -w "%{http_code}\n" localhost:3000     # 200
cd ~/apps/mysado-shop      && git log --oneline -1         # 48차 문서 커밋
cd ~/apps/mysado-shop-prod && git log --oneline -1         # 6d0610c (문서 전용이라 미배포 — 정상)
```

DB 정합 확인 (psql 단독):

```sql
SELECT (SELECT count(*) FROM product) AS total,            -- 228
       (SELECT sum(stock_quantity) FROM product) AS units,  -- 6489
       (SELECT count(*) FROM orders) AS orders;             -- 0
```

---

## 1. 세션 성격이 네 번 바뀌었습니다

47차가 넘긴 과제는 "230건 재고 실물 대조"였습니다. 착수 전 실측이 그 전제를 통째로 뒤집었습니다.

```
① 실물 대조 230건        → 폐기. 허브가 이미 10분마다 수행 중
② 1회 대조 + 가드 UPDATE → 폐기. 허브가 전량 SET 예정이라 현재 재고값은 무의미
③ 상태 축 정책 신규 설계 → 불필요. 37차 모델이 이미 존재하고 런타임 배선도 정상
④ 확정: 매핑 축 확정 + API 사양 확정 + 개봉품 삭제 + 미등록 10건 식별
```

**43차 §8-⑴("착수 전 실측이 설계를 바꾼다")이 여섯 세션 연속입니다.** 이번엔 실측이 바꾼 것이 설계가 아니라 **세션의 존재 이유 자체**였습니다.

---

## 2. 이번 세션 최대의 사실 — 재고 보유자가 셋입니다

문서 어디에도 없던 층이 드러났습니다.

| 층 | 보유자 | 성격 | 주기 |
|---|---|---|---|
| 원장 | **이카운트 ERP** | 일별 확정 재고 (입출고·회계) | 일 마감 |
| 작업 | **통합관리 프로그램**(주승시스템) | 임시 재고 → 확정 가용재고 산출 | **10분** |
| 판매 | 쿠팡·네이버·11번가·지마켓 + **mysado.net** | SET 수신값 | 10분 |

mysado.net은 판매 층의 일원이며, **지금 그 층에서 혼자 끊겨 있습니다.**

**대조의 원천은 이카운트가 아니라 통합 프로그램입니다.** 이카운트가 더 정확해도, 다른 채널이 실제로 받는 숫자는 허브의 확정값입니다. 우리만 원장 값을 쓰면 같은 물건에 채널별로 다른 재고를 광고하게 됩니다. 게이트의 목적은 절대적 진실이 아니라 **채널 간 일관성**입니다.

> **파생 결정 — 34차 §5-4 `StockMovement` 원장은 기각 권고.** 원장은 이카운트가 이미 갖고 있습니다. 우리가 또 만들면 정본이 둘이 되고 어긋날 때 판정 근거가 없습니다. 우리 쪽엔 **원장이 아니라 동기화 로그**(`requestId`·`snapshotAt`·보정분·before/after)만 두면 충분합니다.

---

## 3. DB 변경 — 단순개봉품 2건 삭제

### 3-1. 판정 근거

SKU 중복 2건의 정체가 가격으로 확정됐습니다.

| SKU | 정상품 (유지) | 개봉품 (삭제) | 가격차 |
|---|---|---|---|
| `EF-QS942CTEG` | prod-107 · 22,000 · 재고 4 | **prod-051** · 16,500 · 재고 0 | −5,500 |
| `EF-RS942CBEG` | prod-083 · 88,000 · 재고 2 | **prod-050** · 69,000 · 재고 0 | −19,000 |

Chris 판정: **반품·전시품은 판매 대상이 아니며 이력 보존 불요.** 33차가 확정값 표에 올리고 34차가 품절 처리했던 그 2건입니다.

### 3-2. 실행 조건이 최적이었습니다

47차 테스트 데이터 정리 덕분에 **자식 행이 전부 0**이었습니다.

```
group_id 없음 / group_role 없음 / cart_item 0 / order_item 0 / product_view_log 0
content_status draft / stock 0
```

오픈 후에는 `order_item`이 `SET NULL`이라 상품 없는 주문 이력이 남고 되돌릴 수 없습니다. **47차와 49차 사이의 이 창이 유일한 무해 구간이었습니다.**

### 3-3. 실행·검증

`outputs/48차-개봉품삭제.sql` (28줄) — `DO $$` 가드 블록 4개 + `DELETE ... WHERE id IN (2건)`.

```
가드 ①  product 총행수 = 230
가드 ②  삭제 대상 행수 = 2
가드 ③  삭제 대상 재고합 = 0
가드 ④  자식 행 합 = 0        (cart_item + order_item + product_view_log)
```

`psql -1 -f` 실행 → `DO` / **`DELETE 2`**.

교차검증 7/7:

| 항목 | 기대 | 실측 |
|---|---|---|
| `total` | 228 | ✅ |
| 삭제 대상 잔존 | 0 | ✅ |
| `dup_sku` | 0 | ✅ **SKU 축 1:1 확정** |
| `has_sku` | 178 | ✅ |
| `units` | 6,489 불변 | ✅ |
| 정상품 | prod-083=2 / prod-107=4 | ✅ |
| `product_group` | 25 불변 | ✅ |

재고 총합 불변이 이 삭제가 재고 축에 무영향이었다는 증거입니다.

### 3-4. 🔴 재임포트 제외 목록

34차 §4 INSERT 전용 재임포트 스크립트는 `origin_product_no` 기준이라, **스마트스토어에 개봉품 상품이 남아 있으면 다음 임포트에서 되살아납니다.**

```
13204642205   prod-050  EF-RS942CBEG  러기드 마그넷 개봉품
13204238197   prod-051  EF-QS942CTEG  클리어 케이스 개봉품
```

근본 조치는 **스마트스토어 쪽 판매중지**입니다.

### 3-5. 🔴 절차 위반 기록

파일 생성 명령 말미에 잘못된 문자열이 섞여 나가 `cat -n`이 문법 오류로 죽었고, **파일 내용을 육안 확인하지 못한 채 실행**됐습니다. 47차 5단계 중 ③(사람 검수)이 실질 생략된 것입니다.

안전했던 이유는 가드 블록입니다. `DO`가 예외 없이 통과했다는 것이 네 가드가 전부 실행됐다는 증거이고, `DELETE 2`가 범위를 확증합니다. **34차 §7-2("규칙보다 구조")가 규율의 공백을 메웠습니다.** 사후에 `cat -n`으로 내용이 초안과 일치함을 확인해 ③을 보완했습니다.

> **교훈**: 붙여넣기 명령의 말미 문자를 확인하는 절차가 없습니다. 명령 블록은 `"` 또는 `;`로 끝나야 합니다.

---

## 4. 매핑 축 확정 — `KR` 정규화

### 4-1. 발견

```
CSV:  EF-KS947SCEG
DB :  EF-KS947SCEGKR      ← KR = 한국향 표기 (Chris 확인)
```

`EF-KS947`·`EF-KS948` 8건이 **미등록이 아니라 표기 차이로 매칭 실패**하고 있었습니다. 같은 계열 `EF-KS942`(S26 기본) 4색은 `KR` 없이 매칭됩니다 — **한 계열 안에서 표기가 갈립니다.**

### 4-2. 데이터를 고치지 않고 매칭 규칙으로 해결

| 안 | 문제 |
|---|---|
| A. `sku`에서 `KR` 제거 (UPDATE) | 🔴 **재임포트 회귀.** 원본이 스마트스토어라 다시 붙습니다. 42차 `prod-154` `model_name`과 같은 병 |
| **B. 비교 시점 정규화** ✅ | 원본 보존 + 회귀 없음 + 한국향 정보 유지 |

```
정준 매칭 키 = regexp_replace(sku, 'KR$', '')
적용 지점    = API 재고 SET 조회 / Admin 업로드 조회 — 두 곳뿐
```

### 4-3. 안전성 실측

```
has_sku 178 / KR 접미 17 / KR 제거 후 고유 178  ← 충돌 0
길이 9~14 (max 14 = GP-FPF966HIFRK, RK로 끝날 뿐 KR 아님 — 오절단 없음)
```

### 4-4. 커버리지

| | 정규화 전 | 정규화 후 |
|---|---|---|
| `matched_rows` | 103 | **111** |
| `matched_codes` | 103 | **111** (1:1 유지) |
| `csv_only` | 19 | **11 → 10** (`EF-US937CTE` 제외 반영 후) |
| `db_only` | 75 | **67** |
| `matched_units` | 345 | 362 |

```
111(허브) + 67(sku 보유·수작업) + 50(sku 없음) = 228   ✅
362 + 6,127 = 6,489                                    ✅
```

**데이터 수정 0건으로 커버리지 +8.** 34차가 `sku`를 정준 식별자에서 탈락시킨 두 근거(중복 2·부재 50) 중 중복 축은 §3 삭제로 소멸했고, 부재 50건은 애초에 연동 대상이 아닌 수작업 품목입니다.

---

## 5. 이카운트 CSV 실측 (최종본 = 121행)

```
121행 / 고유 121 / 중복 0 / 창고코드 100 단일 / 기준일자 20260809 (일 단위, 시각 없음)
재고합 614   당일주문합 86   가용(재고−주문) 528
재고 0 : 67건   가용 0 이하 : 68건
```

판본 이력:

| 판 | 행 | 재고합 | 차이 |
|---|---|---|---|
| 초판 | 126 | 663 | — |
| 2판 | 122 | 624 | `IP0001`~`IP0004` 제외 (삼성 부품번호 체계 아님, 39유닛) |
| **최종** | **121** | **614** | `EF-US937CTE` 제외 (재고 미관리 확정, 10유닛) |

**`재고수량`은 당일 주문 차감 전 값입니다.** 예: `EF-CF971CTEG` 주문 47 / 재고 49 → 허브가 **가용 2**를 산출해 각 채널에 전송. 차감 전 값을 그대로 SET하면 47개 초과판매입니다.

**우리 DB 6,489는 명목값이었음이 사실상 확정**됐습니다(이카운트 실물 614, 최대 205 vs 우리 최대 500). 47차 §9의 "6,489가 대조의 출발점"은 폐기하고, 기준선은 `origin_product_no` 228건입니다.

### 5-1. 🔴 파일 규격이 판본마다 달랐습니다

| | 2판 | 최종판 |
|---|---|---|
| 인코딩 | UTF-8 (BOM) | **CP949** |
| 구분자 | 쉼표 | **탭** |
| `품목명` | 빈 값 | 빈 값 |

원인은 **엑셀 편집 경유**로 추정됩니다(Chris). 원인이 특정됐다고 위험이 준 게 아니라 **재발이 확실해진 것**입니다. §8-1 업로드 기능은 인코딩·구분자 자동 판별을 필수로 합니다. 고정 가정은 반드시 깨집니다.

`품목명`이 계속 비어 있어 사람이 코드만 보고 검수해야 합니다. **우승문 대표께 품목명 채워 달라 요청**하면 대조 작업이 크게 빨라집니다.

---

## 6. `csv_only` 10건 — 전부 자사몰 미등록 확정

`exact_hit` 전부 0(KR 정규화 후에도 부재 = 표기 문제 아님), `prefix9_hit`로 복제 가능 여부가 갈립니다.

| 품목코드 | 제품 (삼성 공식 확인) | 재고 | prefix9 | 판정 |
|---|---|---|---|---|
| `EF-XF916SBEG` | **Z 폴드2 5G 아라미드 스탠딩 커버** (2020) | **205** | 0 | 🔴 단종 구형 |
| `GP-FPR640AMIBK` | 버즈4·버즈4프로 케이스 (색상 미확인) | 31 | 6 | ✅ 복제 등록 |
| `GP-FPR640AMJBK` | 〃 | 28 | 6 | ✅ 복제 등록 |
| `GP-FPR640AMGGW` | **버즈4·버즈4프로 헤리티지 케이스 그린** | 25 | 6 | ✅ 복제 등록 |
| `GP-FPS937KDCBK` | **S25 엣지 마그넷 클리어 케이스 블랙** | 0 | 0 | 신규 3색 세트 |
| `GP-FPS937KDCJK` | 〃 (그레이 추정) | 0 | 0 | 〃 |
| `GP-FPS937KDCLK` | 〃 **스카이 블루** | 0 | 0 | 〃 |
| `GP-TOU024SAGBK` | **마그네틱 월렛 블랙** (기종 무관) | 0 | 0 | 신규 계열 |
| `GP-TOU024SAGJK` | **마그네틱 월렛 그레이** | 0 | 0 | 〃 |
| `GP-FPF966HIDYK` | Z폴드7 버라이어티 마그넷 (색상 미확인) | 0 | 4 | 복제 등록 |

미등록 재고 합 **289 / 614 = 47%**. 그중 `EF-XF916SBEG` 단독 205(71%).

### 6-1. 🔴 `916` 기종 판정 정정

세션 중 "916 = S23+"로 전달받았으나 **삼성 공식 지원 페이지 조회 결과 `EF-XF916SBEGKR`은 갤럭시 Z 폴드2 5G 아라미드 스탠딩 커버**입니다.

```
S916 (SM-S916) = S23+          ← 맞음
XF916                          ← S 계열이 아니라 F(폴더블) 계열
우리 DB 형제: XF971=Z폴드8 카본 스탠딩 / XF976=Z폴드8 울트라
```

2020년 출시 제품이며 가격비교 사이트 최저가가 5,900원대까지 내려와 있습니다. **205개는 기회가 아니라 장기 재고**이고, 등록 여부는 매출 기대가 아니라 **소진 전략** 판단입니다.

`937 = S25 엣지`는 확인됐습니다(삼성 공식 `GP-FPS937KDCBK`·`KDCLK` 페이지, 블랙·그레이·스카이 블루 3색).

### 6-2. 등록 우선순위

```
1순위  GP-FPR640 3색 (84개)  — 형제 6종 존재, 복제 등록. 재고 실재
2순위  GP-FPS937 3색         — S25 엣지 현행 기종. 재고 0이나 입고 예상
3순위  GP-TOU024 2색         — 기종 무관 액세서리, 신규 계열
보류   EF-XF916SBEG          — 단종 구형. 소진 목적일 때만
보류   GP-FPF966HIDYK        — 재고 0
```

미등록 10건을 방치하면 10분마다 `SKU_NOT_FOUND` 10건, 일 1,440회가 발생합니다. **조용히 무시하지 않고 항목별 오류로 반환**해야 미동기화가 가시화됩니다.

---

## 7. 확정된 API 사양 (49차 구현 입력)

### 7-1. 🔴 L1 단독 도입 금지 — 재고 push와 주문 수집은 쌍입니다

```
T+0    허브 주문수집 완료 (가용 2 산출)
T+3    자사몰 1개 판매 → 로컬 차감 2→1
T+3.5  SET(2) 도착 → 1→2 부활        ← 자사몰 판매분 소거
       허브가 그 2를 쿠팡·11번가·지마켓에도 push  ← 오염 전 채널 전파
```

지금은 mysado.net이 끊겨 있어 오염이 격리돼 있습니다. **재고 방향만 연결하면 격리가 풀립니다.**

### 7-2. 방어 2겹

**⑴ 클라이언트(허브) — 10분 잡 내부 순서 고정** (전달 완료)

```
① GET /api/v1/orders   자사몰 신규 주문 수집   ← 반드시 먼저
② 허브 재고 계산
③ POST /api/v1/inventory/bulk (SET)          ← 나중
```

**⑵ 서버(우리) — `snapshotAt` 보정**

```
최종값 = 허브가 보낸 수량 − (snapshotAt 이후 자사몰 PAID 주문 수량 합)
```

- **정의**: 허브가 그 회차 `GET /orders`에서 받은 **마지막 주문의 `updatedAt`(= 다음 커서값)**. "허브가 아직 못 본 주문"이 정확히 그 이후 집합이라 근사가 아니라 정확합니다
- **생성 주체**: 허브 (생성·전송 가능 확인됨)
- **폴백**: 미제공 시 서버가 해당 클라이언트에 마지막 발급한 커서를 대용. 순서 고정이 지켜지면 항상 실제 계산 시각 이하라 안전 방향
- **오차 방향**: 이른 쪽이 안전(이중 차감 = 판매 기회 손실) / 늦은 쪽이 위험(소거 = 초과판매)
- **이중 차감 없음**: 허브가 이미 반영한 주문은 `snapshotAt` 이전이라 보정 집합에서 제외됨
- **파생**: 이 보정이 있으면 34차 §5-6의 낙관적 잠금(`Product.version`)은 **불필요**

Chris 의견은 "다음 회차 SET으로 결국 수렴하므로 보정 불요"였으나, 수렴까지 **최대 10분간 재고가 부풀어 있는 창**이 남습니다. 허브가 `snapshotAt`을 보낼 수 있으므로 비용 0으로 그 창을 닫습니다.

**남는 리스크 1개(수용)**: 자사몰과 타채널에서 동시에 마지막 1개가 팔리는 경우. 채널 간 재고 예약 없이는 어떤 설계로도 못 막습니다. 이것만이 진짜 "향후 과제"입니다.

### 7-3. 확정 항목

| # | 항목 | 확정 |
|---|---|---|
| 1 | 식별자 | `product.id`. 허브는 SKU로 보내고 **우리가 `KR` 정규화 후 해석** |
| 2 | 전송값 | 통합 프로그램의 **확정 가용재고 1개 필드**. 이카운트 `재고수량`(차감 전) 아님 |
| 3 | 창고 | 100 단일. 다창고 발생 시 허브가 합산해 전송 (우리는 창고 개념 미보유) |
| 4 | mode | `SET` |
| 5 | 호출량 | 10분 × 2호출 = 일 288회. rate limit 분당 60이면 충분 |
| 6 | 미매칭 | 항목별 `SKU_NOT_FOUND` 반환. **조용한 무시 금지** |
| 7 | `SKU_AMBIGUOUS` | **경로 삭제** — §3 삭제로 중복 소멸 |
| 8 | 망 | `/api/v1`은 Tailscale·사내망 IP만 허용 |
| 9 | 송장 API | 오픈 초기 admin 수기 갈음 가능. 2차 |

### 7-4. 우승문 대표 전달 완료

```
① 10분 잡 순서 고정 (주문 수집 → 재고 계산 → SET)
② EF-US937CTE 재고 미관리 — SET 대상 제외 요청 → 최종 CSV에 반영 확인됨
③ KR 접미는 동일 코드로 취급 — 허브는 현행 유지 (변경 불필요)
```

추가 요청 후보: **CSV `품목명` 채우기**(§5-1).

---

## 8. 🔴 게이트 ① 재정의 — 잔여 범위 확정

| 구간 | 행 | 유닛 | 해결 경로 |
|---|---|---|---|
| 허브 자동 연동 | **111** | 362 | API (49차) |
| **수작업 관리** | **117** | **6,127** | 🔴 **미해결** |

게이트 ① 폐쇄 조건이 바뀌었습니다.

```
구: 재고 실물 대조 완료
신: API 구현 + 허브 최초 SET 수신·교차검증 + 수작업 117행 재고 실물 확정
```

**대가**: 오픈이 API 완성에 종속됩니다. 34차가 자체 API를 39차~로 미룬 근거("검증할 트래픽이 없다")는 주문 방향에만 해당했고, 재고 방향은 허브가 지금도 10분마다 계산 중이라 **트리거가 이미 도래해 있었습니다.**

### 8-1. 수작업 117행의 재고 수정 수단이 없습니다

Chris 확인: 별도 배송체계 품목이며 **Admin 또는 관리자가 수작업 관리**. 그런데 현재 그 수단이 구현돼 있지 않습니다(`03_admin-dashboard.md`에 "재고 일괄 수정" 설계만 존재, 구현 미확인).

**요청 사양: SKU별 재고를 CSV/엑셀로 업로드해 일괄 수정.**

핵심 설계 — **파일을 사람이 만들지 않습니다.**

```
① 내보내기  Admin이 현재 재고가 채워진 파일 다운로드
② 편집      new_stock 열만 수정
③ 업로드    같은 파일
④ 미리보기  변경 예정 목록 + 경고 (dry-run)
⑤ 확정      단일 트랜잭션 반영 + admin_audit_log
```

id를 시스템이 넣으므로 오타·매칭 실패가 원천 소멸하고, 42차 `expectedCurrent` 가드가 파일 안에 자연히 들어갑니다.

| 열 | 성격 |
|---|---|
| `id` | 🔒 `prod-NNN` 정준 키 |
| `sku` / `name` | 참고 (sku는 50건 부재이므로 키로 쓸 수 없음) |
| `current_stock` | 🔒 내보내기 시점 값 = `expectedCurrent` |
| `new_stock` | ✏️ 비우면 변경 없음 |
| `reason` | ✏️ 감사 로그 기록 |

검증 6단계: V1 필수열 / V2 정수·음수불가 / V3 파일 내 id 중복 / V4 DB 존재 / **V5 `current_stock` 대조(자동 보정 금지)** / **V6 허브 연동 상품 차단**.

파싱 방어(§5-1 근거): **인코딩 UTF-8(BOM 유무)·CP949 자동 판별 / 구분자 쉼표·탭 자동 판별 / 열 이름 매핑(순서 의존 금지) / 파싱 직후 행수·필수열을 사람에게 먼저 표시**.

반영 규칙: `lib/inventory.ts` 경유(43차 원칙) · 단일 트랜잭션 전량 원자성 · `status`·`is_active` 자동 변경 없음 · 상한 500행 1MB.

### 8-2. 선행 스키마 1개 — `inventory_source`

```prisma
inventorySource String @default("MANUAL") @map("inventory_source")  // HUB | MANUAL
```

| 값 | 대상 | 쓰기 주체 | 다른 경로 |
|---|---|---|---|
| `HUB` | 111행 | 허브 SET (10분) | Admin 업로드 **차단** |
| `MANUAL` | 117행 | Admin 업로드 | API SET **400 거부** |

기본값 `MANUAL`이 안전 방향입니다(신규 상품이 실수로 허브 관리로 분류돼 재고가 덮이지 않음). 초기화는 `KR` 정규화 키 기준 UPDATE 111건, 사전 SELECT로 111 확인 후 영향 행수 대조.

**이 컬럼이 API(49차)와 Admin 업로드 기능의 공통 선행입니다.** additive 컬럼 1개라 규약 부합.

---

## 9. 제 판독 오류 5건 (기록)

이번 세션에서 제가 문서·부분 실측·전언을 근거로 단정했다가 실측에 뒤집힌 건들입니다.

| # | 잘못된 단정 | 실제 |
|---|---|---|
| ① | `SOLD_OUT` + `is_active=true` 46건은 오류 | `deriveIsVisible`의 **설계된 값**. 품절도 노출 유지가 37차 ② 결정 |
| ② | 상태 축 3분할 정책을 신규 설계해야 함 | 37차 `AUTO_TRANSITION`·`isAutoTransitionable`에 **이미 존재** |
| ③ | 자동 전이 술어의 런타임 호출부 0건 → 게이트 신규 항목 | 🔴 **grep 패턴에 `isOnSaleStatus`·`getStatusBadgeLabel`·`getUnavailableLabel`을 넣지 않았음.** 실제로는 런타임 4곳이 정상 import |
| ④ | 46건에 재고가 들어와도 안 팔림 | 판정이 `stock`을 직독하므로 정상 판매 |
| ⑤ | `EF-XF916SBEG` 205개는 "가장 큰 단건 기회" | **2020년 Z폴드2용 단종품.** 삼성 공식 조회로 정정 |

**③이 가장 나쁩니다.** "0건 판정 전 역증명"이라는 절을 스스로 쓰고도, `scripts/`에서 26줄 잡혔으니 패턴이 작동한다는 **잘못된 논거로 통과**시켰습니다. 패턴의 일부가 작동하는 것과 필요한 항목이 다 들어 있는 것은 별개입니다.

**⑤는 성격이 다릅니다** — 전언(916=S23+)을 검증 없이 받은 것이 아니라, 검증을 요청받고서야 정정됐습니다. SKU 기종 코드는 접두 문자(`S`/`F`)까지 봐야 하고, 숫자만으로 기종을 판정하면 안 됩니다.

> **규약 (48차 신설 2건)**
> ⓐ grep 0건의 역증명은 "패턴이 다른 곳에서 히트했는가"가 아니라 **"검사 대상 심볼이 패턴에 전부 들어 있는가"**로 한다. 모듈 사용 여부는 심볼 열거가 아니라 **import 경로 문자열**을 먼저 건다.
> ⓑ SKU 기종 코드는 **숫자 앞 문자까지 포함해 판정**한다. `XS916`(S23+)과 `XF916`(Z폴드2)은 다른 제품이다.

---

## 10. 확인된 현행 동작 (변경 불요)

재고 0 상품의 구매 차단은 **이미 5중으로 걸려 있습니다.** 상태 자동 전이가 배선되지 않았어도 무해합니다 — 판정이 모두 `stock`을 직접 읽기 때문입니다.

| 층 | 파일 | 판정식 |
|---|---|---|
| 목록 뱃지 | `components/products/ProductCard.tsx:14` | `getStatusBadgeLabel(status, stock_quantity)` → "품절" |
| 상세 구매 | `app/products/[id]/page.tsx:92` | `isOnSaleStatus(status) && stock_quantity > 0` |
| 장바구니 | `lib/cart.ts:41` | `isVisible && isOnSaleStatus && stock >= quantity` |
| 주문 생성 | `lib/orders.ts:76` | `isOnSaleStatus(item.product.status) && …` |
| 결제 | `lib/inventory.ts` (43차) | `WHERE stock_quantity >= n` → 409 |

**34차 §5-4(재고 0에서 `status`·`is_active` 자동 변경 금지)가 현행 동작이며 유지합니다.** 37차 주석의 "재고=0에서 자동 전이"는 런타임에 배선되지 않았고, 표시 문구가 양쪽 경로 모두 "품절"이라 화면 차이도 없습니다.

문서 오기 1건: 34차가 품절 상태를 `OUTOFSTOCK`으로 기록했으나 **실제 값은 `SOLD_OUT`**. `lib/product-status.ts` 상수가 정본입니다.

---

## 11. 이월 항목

### 11-1. 신규 (48차 발생)

| # | 항목 | 비고 |
|---|---|---|
| 1 | **`inventory_source` 컬럼** | 49차 API의 선행. additive 1개 |
| 2 | **Admin 재고 일괄 수정 (CSV 업로드)** | §8-1. 수작업 117행의 유일한 갱신 수단 |
| 3 | **미등록 10코드 등록** | §6-2 우선순위. 1순위 `GP-FPR640` 3색(84개) |
| 4 | **재임포트 제외 목록** | `13204642205`·`13204238197`. 스마트스토어 판매중지가 근본 조치 |
| 5 | **수작업 117행 6,127유닛 실물 확정** | 게이트 ① 잔여. 명목값 가능성 높음 |
| 6 | CSV 규격 변동 | §5-1. 인코딩·구분자 자동 판별 필수 |
| 7 | 명령 말미 오염 | §3-5. 붙여넣기 전 마지막 문자 확인 |
| 8 | grep 역증명 / SKU 기종 판정 규약 | §9 ⓐⓑ |

### 11-2. 해소

- ✅ **SKU 중복 2건** — 개봉품 삭제로 소멸. `SKU_AMBIGUOUS` 경로 불요
- ✅ **42차 `prod-050` `content_meta` 시각 이상건** — 행 삭제로 소멸
- ✅ **33차 확정값 표의 품절 2건** — 삭제 완료

### 11-3. 기존 이월 (승계)

- `admin_audit_log` 미점검 (47차) — **§8-1 업로드 기능이 첫 실사용**
- `product.status` DEFAULT 구값 `'SALE'` / `layer3b-write.js` V6 전역 유일성
- `deriveIsVisible` 복제 3곳 — `product-promote-write.js:56`·`product-insert-38.js:62`·`status-backfill.js:64`. **`product-promote-write.js:54`는 `VISIBLE_VALUES`에 구값 `'SALE'` 잔존**. 전부 `scripts/` 로컬 재정의라 런타임 무관
- 42차 절단 18건(`라이트` 10·`다크` 8) / `라이트블루` 3건 / `prod-066` 투명
- prod-011 `로열핑/뱅` 오타 / prod-154 `model_name` / `variant_label` 40자 절단
- 7-3 게시 게이트(draft→review→published) / mysado.co.kr 301 / 소셜로그인
- 웹훅 두 경로 미검증 / Prisma 트랜잭션 타임아웃 5초
- 장바구니 수량 직접 입력 / `app/api/mypage/addresses/*` raw SQL 3건

### 11-4. 🔴 오픈 게이트 4건

| # | 게이트 | 상태 |
|---|---|---|
| ① | **API 구현 + 허브 최초 SET 수신·교차검증 + 수작업 117행 재고 확정** | 🔴 49차 |
| ② | 오프사이트 백업 | 🔴 미착수 |
| ③ | 다운타임 알림 | 🔴 미착수 |
| ④ | 라이브 웹훅 실발신 확인 | ⚠️ 토스 심사 통과 후 |

---

## 12. 산출물

| 경로 | 내용 |
|---|---|
| `outputs/48차-개봉품삭제.sql` | 28줄, 가드 4 + DELETE 2행 |
| `~/db-backups/mysado_db-2026-08-10-pre-48delete.sql` | 650K / 15테이블 / product 230 실측 |
| 본 핸드오프 | `outputs/` |

DB write 1건(행 삭제). **런타임 코드 무수정 — 빌드·재시작 없음.** 마이그레이션 0건.

---

## 13. 49차 착수 권고

**A. `inventory_source` 마이그레이션 + 재고 SET 수신 API** ← 권장

게이트 ①의 본체입니다. 순서:

```
① inventory_source additive (--create-only → SQL 육안 → migrate deploy)
② 초기화 UPDATE 111건 (KR 정규화 키 기준, 사전 SELECT로 111 확인)
③ API Key 인증 + 공통 응답 구조 + Nginx 사내망 제한
④ POST /api/v1/inventory/bulk (SET, snapshotAt 보정, 항목별 부분 성공)
⑤ GET /api/v1/orders (읽기 전용, (updatedAt, id) 복합 커서)
```

④⑤는 쌍이므로 한 세션에 들어가지 않으면 **④ 없이 ⑤만 배포**하는 것이 안전합니다(읽기 전용이라 무해). 역순은 금지입니다.

**B. Admin 재고 일괄 수정** — §8-1. 수작업 117행의 유일한 갱신 수단이며 A와 `inventory_source`를 공유합니다.

**C. 미등록 등록** — 34차 §4 INSERT 전용 스크립트 재사용. `GP-FPR640` 3색부터(형제 복제라 가장 쉽고 재고 84개가 실재).

> **마감**: Toss 라이브 계약 2026-08-16 (5일). 심사 결과에 따라 8-3 묶음(키 교체·폴백 제거·010 가드)이 끼어들 수 있습니다.
